# tree — Project Context / Handoff File

**Read this file first. It is the single source of truth for resuming this project.**
If you are an AI (or person) picking this up for the first time, read this entire file, then skim the files listed in [File Map](#file-map) before writing any code.

> How this works: every session that works on this project should END by updating this file (the "Status" and "Next Steps" sections), so a future session or a different AI can start/restart from here at any time.

> **LATEST VERIFICATION:** Full hosted multiplayer is implemented with public/password lobbies, five-character codes, chunked join/reconnect snapshots, host-authoritative world/enemy/boss simulation, synchronized players, tiles, walls, entities, projectiles, pickups, chests, inventories, crafting, progression and weather, safe guest spawn selection, stale-player cleanup, bounded automatic reconnect, and guest promotion on host loss. The Cloudflare Durable Object relay is deployed as version `e06de091-a6b3-4e1c-8e45-8e7308b6d980` with protected stuck-lobby removal; the orphaned `Oak` test lobby was deleted and the production listing verified empty. Relay helper/dry-run checks and a two-browser full-world integration test including an obstructed spawn and forced disconnect/rejoin all pass. **Expert/Master difficulty modes, Minecraft-style world patch migration (Batch 62), difficulty fidelity (Batch 63), specialized combat (Batch 64), vanilla enemy roster (Batch 65), NPC services (Batch 66), Angler reward parity (Batch 67), and the persistent 250-entry Zoologist bestiary (Batch 68) are implemented; the latest bestiary suite passes 29/29 real-page checks**, with all JavaScript files passing `node --check`.
> **LATEST UI/TERRAIN REPAIR:** How To Play is now a structured, independently scrollable field guide on desktop and mobile; inventory stacks support drag-and-swap while preserving metadata; fresh-world spawn flattening no longer leaves floating grass or underground air bands; the Guide starts above ground; caves preserve ocean liquids; and mini-biomes/Aether generate actual chambers instead of solid disks. A follow-up terrain hotfix reduces extreme surface swings, prevents random caves from breaking through the upper crust, gives spawn a twelve-tile foundation with gradual approaches, and fixes the contradictory `Sky`/`Forest (Sky)` HUD labels at ground level. The original real-page suite passes 13/13 checks and the follow-up full-world suite passes 32/32. The player renderer's missing outer canvas restore is also repaired, eliminating accumulating camera transforms and stale duplicate frames. A real menu-driven playthrough then exposed and repaired the remaining flat-horizon background fault: sky/underground layers now follow every visible terrain column without rectangular bands. Terraria-style lighting now keeps underground terrain opaque unless reached by actual light, removes the free player glow, supports held/placed lights, and exposes only five emissive ore types; the focused lighting suite passes 11/11 checks.

---

## 1. What this is

`tree` is a **full-progression Terraria clone** written in **plain JavaScript (ES5-era style, no build step, no framework, no dependencies)**. It runs entirely in the browser as static files.

- **Two-phase progression**: the game starts in **pre-hardmode** (copper/silver/gold ore, **one evil biome per world — Corruption OR Crimson, player-selectable at world creation (Random/Corruption/Crimson)** with its matching stone + demonite/crimtane ore + enemies) and transitions to the existing **hardmode** phase (Cobalt → Mythril → Adamantite, mechanical bosses The Twins / The Destroyer / Skeletron Prime, Hallowed gear) once the Wall of Flesh is defeated, all the way through the Moon Lord victory.
- Entire game is **procedurally generated** from a seed (world + tile data). No assets; all art is drawn programmatically on `<canvas>`; all sound is synthesized with WebAudio.
- Open `index.html` directly in a browser (e.g. double-click, or `npx serve` / VS Code Live Server). No server or build needed.

> **CURRENT STATE:** `js/main.js` **exists** and the game **runs** (verified headless, **60/60 Batch 59 vanilla-enemy/treasure-hunting checks PASS on the latest session** with zero console errors — 7 new vanilla enemies (Sand Slime, Giant Worm, Nymph, Digger, Crimslime, Slimeling, Moth) reachable through a world-sweep of their real `pickEnemy` pools with one-evil isolation intact, 2 new accessories (Metal Detector HUD readout, Butterfly Wings), every new drop table valid, Crimslime splits into Slimelings, both worms segment-render; the prior 18/18 Shimmer-catalog audit checks remain passing, the earlier furniture-decrafting suite passed 39/39, equipment-decrafting passed 25/25 and bar-decrafting passed 8/8, and the earlier expanded-deployed-magic suite passed 20/20, explicit-magic-volley suite passed 20/20, class-accessory-damage suite passed 20/20, Mimic/pet-acquisition suite passed 22/22, boss-reward-acquisition suite passed 23/23, ranged-summon suite passed 18/18, summon-staff/contact-minion suite passed 18/18, special-whip suite passed 20/20, whip/minion-integrity suite passed 20/20, Life Drain suite passed 22/22, projectile-status suite passed 25/25, guided/piercing suite passed 25/25, launcher/proximity-mine suite passed 19/19, ricochet/explosive suite passed 19/19, terrain-targeted suite passed 22/22, deployed-magic suite passed 23/23, channelled-magic suite passed 24/24, controlled/payload-melee suite passed 25/25, yoyo suite passed 23/23, flail suite passed 16/16, spear suite passed 16/16, melee-projectile suite passed 16/16, recipe/ranged-behavior suite passed 19/19, progression/acquisition-integrity suite passed 23/23, Guide-service suite passed 18/18, Angler-quest suite passed 17/17, town-rescue suite passed 27/27, complete town-economy suite passed 21/21, town-housing suite passed 20/20, full town-roster arrival suite passed 21/21, persistent core-arrival suite passed 22/22, expanded town-service suite passed 28/28, core town-service suite passed 27/27, Solar Eclipse progression suite passed 25/25, Lunar Pillar elite suite passed 22/22, Old One's Army integrity suite passed 26/26, Martian Madness suite passed 30/30, Blood Moon roster/fishing suite passed 23/23, moon-roster/reward suite passed 21/21, invasion-integrity suite passed 23/23, secondary-event/weather suite passed 41/41, Old One's Army suite passed 44/44, early-event/boss suite passed 38/38, progression-integrity suite passed 33/33, save-system suite passed 24/24, and biome-spread suite passed 33/33). Full progression, major events, town systems, saves, and broad content remain working while Terraria 1.4.4 parity expansion continues; see `PARITY.md` and [Status](#6-status).

---

## 2. How to run

1. Open `index.html` in a browser (Chrome/Edge/Firefox).
2. Or run a local static server from the project folder: `npx serve .` then open the printed URL.

Controls (documented in the game's How To Play):
- Move: `A`/`D` or arrows · Jump/glide: `Space`/`W` (multi-jump wings after release) · Run: `Shift`/`Ctrl`
- Mine/Attack: Left click · Place/Interact: Right click · Hotbar: `1`–`0` or scroll
- Inventory/Craft panel: `E` · Guide: `H` · Drop: `Q` · Pause: `Esc` · Save: `Esc` → Save & Quit
- Bosses: craft a Mechanical item, select it, click to summon.

---

## 3. File map

| File | Size | Purpose |
|---|---|---|
| `index.html` | 3.2 KB | Page shell: canvas, HUD divs, panel tabs (incl. chest storage + Pylon network pages), main menu (with **evil biome selector: Random/Corruption/Crimson**), death screen, loading overlay. Loads all scripts **in order** (see list below). |
| `css/style.css` | 9.3 KB | All styling: HUD, hotbar, panel, overlays, bars, dye/chest grids. |
| `js/const.js` | 5.4 KB | Global enums/constants: `TILE`, `T` (tile ids, incl. CRIMSON/UNDERWORLD/AETHER tiles + GLASS/SPOOKYWOOD/HONEY/CHEST/CHAIR/TABLE + COPPER/SILVER/GOLD/DEMONITE 52–55 + DUNGEONBRICK/DUNGEONDOOR/TIN/LEAD/TUNGSTEN/PLATINUM/METEORITE/SANDSTONE 56–63), `WALL`, `BIOME` (20 ids incl. CRIMSON/UNDERWORLD/SPIDER/GRANITE/MARBLE/AETHER/DUNGEON/UNDERDESERT/UNDERSNOW), `TILE_HARD`, `TILE_DROP`, `E` (enemy ids, incl. 6 event bosses + 13 biome enemies + DEMONEYE/CAVEBAT/GOBLIN/CORRUPTCRIMSONFLYER 163–166 + 8 dungeon/desert/snow enemies 167–174), `P` (projectile ids, incl. DART/FROSTBOLT/PLASMA), `Dmg`, `KEY`, `KEY_JUST`, `MOUSE`, physics constants, `VERSION`. |
| `js/util.js` | 2.7 KB | `mulberry32` seeded RNG, value noise (`makeNoise1D/2D`), `clamp`, `lerp`, `dist`, `hexToRgb`, `util.delta()`, `makePool(max)`. |
| `js/audio.js` | 3.4 KB | `AudioSys` — synthesized SFX via WebAudio (`tone`, `noise`, `play(name)`). |
| `js/items.js` | 90.7 KB | Item ids `I`, item defs `ITEMS`, `defItem()` helper, `TILE_ITEM` reverse map, `Inventory` class (50 slots, armor, accessories[6], dyes[6], add/consume/removeAt/defense/accEffects incl. `minion`/`jumps`/`noFall`). Hooks/mounts/fishing/bait/crates/pets/light-pets/event items/dyes/rockets/darts/event weapons+blocks+potions + biome blocks (crimstone/crimtane/ash/hellstone/hellbrick/cloud/granite/marble/obsidian) + Starfury + pre-hardmode ores/bars/tools/weapons/armor + 7 boss summons. |
| `js/world.js` | 17.4 KB | `World` class: tile/wall/hp Uint arrays, collision queries, lights, `generate(hardmode, evil)` (procedural gen: biomes, temple + `templeCenter`, `planteraBulbs`, ores gated by phase, water, **Underworld/hellY+lava+hellbrick+hellstone, sky islands `skyIslands`, spider/granite/marble cave columns, Aether `aetherPos` shimmer pool**), spawn point, guide position. `world.evil` = `'corrupt'`/`'crimson'` (chosen/random at gen) drives both evil surface slots + ebon/crim stone + demonite/crimtane ore. Chest storage map + spill helpers + `startHardmode()` (injects hardmode ores) + `biomeAt` (surface bands + SKY/UNDERWORLD/mini-biome/AETHER aware) + `isLavaAt`/`isShimmerAt`/`liquidAt`. |
| `js/crafting.js` | 51.2 KB | `RECIPES`, `recipe()` helper, `stationsForPlayer`, `recipeAvailable`, `craftRecipe`, special placements. ~everything craftable incl. hooks/mounts/rods/bait/crates/dyes/rockets/darts/event items/furniture + pre-hardmode smelting/gear/summons. |
| `js/entities.js` | 42.1 KB | `physicsStep` (AABB collision), `ENT_DEF`, `makeEntity`, `spawnEntity`, `hitEntity`/`killEntity`, `dropTable(type, game)` (hm-aware souls), per-type AI steps (~25 enemies + 6 event foes + 4 pre-hardmode foes), `initSegments`, minions (`spawnMinion`/`minionStep`). |
| `js/bosses.js` | 32.4 KB | All 10 boss spawns/AI + **6 event bosses** (`spawnMourningWood`, `spawnPumpking`, `spawnEverscream`, `spawnSantank`, `spawnIceQueen`, `spawnMartianSaucer`) + **6 pre-hardmode bosses** (King Slime, Eye of Cthulhu, Eater of Worlds, Brain of Cthulhu, Queen Bee, Skeletron, Wall of Flesh), `bossStep`, `bossHit`, `segmentHit`, `armHit`, `killBoss` + drops, `onBossDefeated` hook. |
| `js/player.js` | 26.7 KB | `Player` class: movement, swimming, multi-jump wings, mounts, grappling hooks, fishing (cast/bite/reel), pets & light pets, accessory effects, mining/melee/ranged/magic/whip/minion/consumable/summon/place/event actions, damage/death. |
| `js/render.js` | 74.1 KB | All canvas drawing: sprite generation, background/sky, tiles, entities, bosses (incl. 6 event bosses + 6 pre-hardmode bosses), player (mounts/hook line/dyes), projectiles, FX (incl. whip/teleport/fishing bobber), lighting (light pets), item icons, minimap. Global `Time`. |
| `js/oldonesarmy.js` | new | Isolated Old One's Army subsystem: Eternia Stand arena validation, fixed portals/crystal objective, three progression tiers, eight Etherian enemies, Dark Mage/Ogre/Betsy, transient Etherian Mana, Defender Medals, four sentries, tier rewards, rendering, and Tavernkeep shop progression. |
| `js/main.js` | 52.9 KB / ~1,580 lines | The game engine: `buildGame`/`startNewGame`, fixed-timestep loop, input, camera, ambient spawning, event driver (`startEvent`/`updateEvents`/`startWave`/`spawnEventEnemy`/`endEvent`), chest UI (`tryOpenChest`/`renderChest`), projectiles, pickups, FX, HUD (incl. `#eventhud`), panels (incl. chest + dye slots), boss routing (all 16 + 7 pre-hardmode), Plantera bulbs, victory overlay, `startHardmode()`, hm-gated `pickEnemy`/`spawnNpcs`. `buildGame(seed, evil)`/`startNewGame` read the menu's evil selector. |
| `js/achievements.js` | 6.2 KB | `ACHIEVEMENTS` map (84 entries), `Achievements.unlock(id, game)`, `renderAchievements()` — persisted with the localStorage world save. |
| `PARITY.md` | small | Staged Terraria 1.4.4 parity backlog. Progression-critical repairs first, then Hardmode encounters, early events/bosses, Old One's Army, weather/events, and long-term catalog parity. |

Script load order in `index.html` (this matters — later files use globals defined in earlier ones):
`const.js → util.js → audio.js → items.js → world.js → crafting.js → entities.js → bosses.js → player.js → render.js → oldonesarmy.js → achievements.js → main.js`

---

## 4. What main.js must do

This is the intended contract — every other file assumes these exist on a global `game` object. Build `js/main.js` to satisfy ALL of the following:

### 4.1 Game object fields (used across the codebase)
- `game.world` — `new World(W, H, seed)` with `generate()` called; also `world.nearbyStations = []` refreshed each frame (see `findStations` in `world.js:111`, used by crafting).
- `game.player` — `new Player(world)`; call `starterItems()` for a fresh world.
- `game.cam` — `{x, y}` camera (world px), centered on player each frame (smooth lerp is nice).
- `game.entities` — array (players, enemies, bosses, guide, arms).
- `game.projectiles` — `makePool(...)` instance.
- `game.pickups` — array of `{item, count, x, y, seed, t}` (see `addPickup` below).
- `game.fx` — visual effects array; each `{type, x, y, t, max, seed, ...}`. `drawFX` uses `f.t/f.max` so `max` must be set when pushed, or set `f.max` = starting `t`.
- `game.fxTexts` — floating combat text array; each `{x, y, text, t, color}`.
- `game.bossBars` — array of `{id, name, hp, maxHp, color}` (see `makeBoss` in `bosses.js:15`).
- `game.bossesDefeated` — `{}` keyed by `'twins' | 'destroyer' | 'skelprime'` (set in `killBoss`).
- `game.clouds` — array for parallax clouds (render loop reads `game.clouds[i].x/y/w/h/parallax`).
- `game.timeOfDay` — `0..1` (0 = midnight, 0.5 = noon). Advances slowly; drives day/night sky & lighting.
- `game.lightCanvas`, `game.minimap` — lazily created canvases (render.js handles these).
- `game.panelOpen` — bool; when true, player update skips mouse actions and panels show.
- `game.world.dirty` — set `true` after tile changes so minimap can rebuild (see `buildMinimap`).
- `game.hardmode` — bool (false = pre-hardmode). Gates ore gen (`world.generate(game.hardmode)`), enemy pools (`pickEnemy`), soul drops (`dropTable(type, game)`), NPC roster (`spawnNpcs`). Set true by `game.startHardmode()`. `game.world.generate(game.hardmode)` is called at world creation.

### 4.2 Game methods (called from other files — implement ALL)
- `game.message(text)` — show a HUD message (`#message` div, fade out).
- `game.spawnFloatingText(x, y, text, color)` — push into `fxTexts`; decay `f.t`.
- `game.spawnMinePuff(x, y)` — small break particles (push into `fx`).
- `game.addPickup(x, y, id, count)` — push pickup with random `seed`; magnetize to player; auto-add to inventory when close (`Inventory.add`); play `AudioSys.play('pickup')`.
- `game.hitBoss(e, dmg, kbx, kby)` — delegates to `bossHit(e, dmg, kbx, kby, game)`.
- `game.damagePlayer(dmg, from, kbx)` — calls `player.damage(dmg, from, kbx)`.
- `game.flash()` — trigger `#screenflash` opacity flash.
- `game.respawn()` — reset player at spawn, clear dying, restore hp/mana.
- `game.deathscreen()` — show `#deathscreen` overlay.
- `game.spawnBoss(bossId)` — route to the pre-hardmode boss spawns (kingslime/eyeofcthulhu/eaterofworlds/brainofcthulhu/queenbee/skeletron/wallofflesh) or hardmode ones (`spawnTwins(game)` / `spawnDestroyer(game)` / `spawnSkeleton(game)` etc.); also needed by `trySummon` (delay 800ms then call).
- `game.anyBossAlive()` — true if any boss entity alive (`e.boss && !e.dead`).
- `game.onEnemyKilled(e)` — progress hooks (e.g. spawn more / messages). Called by `killEntity`.
- `game.checkBossCompletion()` — after all three `bossesDefeated` are true, show a victory message/state.
- `game.placeSpecial(special)` — for `'workbench' | 'furnace' | 'anvil'`: place the station in an adjacent free tile (like `Player.tryPlace` logic) and return true/false.
- `game.startHardmode()` — flip `game.hardmode` to true, message + roar, `world.startHardmode()` ore injection, kill the guide, re-run `spawnNpcs()` for the hardmode-only NPCs. Triggered from `onBossDefeated('wallofflesh')`.

### 4.3 Loop, input, UI wiring
- `requestAnimationFrame` loop updating: `Time.seconds`/`Time.frame`, `Time.seconds += dt`, world entities (`enemyStep`/`bossStep` per entity), projectiles (`update` fn handles movement + collision vs tiles + enemies + player), pickups, fx/fxTexts decay, player update, camera, day/night, `world.nearbyStations = world.findStations(player.x, player.y)`, HUD, minimap dirty checks.
- Keyboard: fill `KEY` (held) and `KEY_JUST` (pressed-this-frame) from `keydown`/`keyup`. Handle `E` (toggle panel), `H` (guide), `Q` (drop selected), `Esc` (pause/menu), `1`–`0` (hotbar select).
- Mouse: update `MOUSE.x/y` (screen), `MOUSE.wx/wy` (world via camera), `MOUSE.down/right` on `mousedown/up`, `MOUSE.wheel` for hotbar scrolling; `MOUSE.tpx/tpy` (tile-preview for `#tilehint`).
- Menus: `#btn-new` → generate new world (show `#loading` + `#loadprogress` during gen since it's synchronous), hide main menu; `#btn-continue` (currently hidden — save system not implemented) → load save; `#btn-howto` toggles `#howto`; `#btn-respawn` → `respawn()`.
- Panel tabs: switch `.panel-page` visibility; render `#panel-inventory` (50 cells + armor + desc), `#panel-crafting` (categorized recipe grid using `stationsForPlayer`/`recipeAvailable`; click to `craftRecipe`), `#panel-guide`.
- Hotbar: render `#hotbar` (10 slots from `inventory.slots[0..9]`) + selection highlight + counts.
- HUD each frame: `#clock` (from `timeOfDay`), `#depth`, `#biome`, `#fps`, `#hpbar`/`#manabar` widths, `#bossbars` from `game.bossBars`, `#crosshair` position, `#interact` prompts, `#message`.
- Resize: set canvas size to window; `canvas.width/height` must match CSS for correct drawing.
- Call `AudioSys.init()` on first user gesture (browser autoplay policy) and `AudioSys.resume()` on resume.

### 4.4 Spawning / world lifecycle
- Spawn a `Guide` entity at `world.guidePos` (type `E.GUIDE`).
- Ambient enemy spawning near the player per biome/depth — **pre-hardmode** (`!game.hardmode`) = weak pools (slimes, zombies, demon eyes, cave bats, goblins, jungle/snow/corrupt/crimson critters); **hardmode** = hard enemies like Armored Zombie, Wraith, Pixie, Unicorn, Chaos Elemental, Gastropod, Wyvern at sky depth, etc. — see `ENT_DEF`). Corrupt/Hallow enemies in their biomes; night spawns during night.
- Fall-out-of-world handling: `player.update` already calls `die()` when too low (`player.js:132`).

---

## 5. Conventions

- **Style:** ES5 — `var`, `function` constructors + `Prototype.method = function(){}`, no `let`/`const`/classes/arrow functions. **Follow this.** Global constants uppercase; global state lowercase.
- **Globals are shared intentionally** (no modules, single script scope). Don't shadow them.
- **Units:** positions/sizes in pixels; tiles are `TILE`=16px. Entity `x,y` = **center**; player `x,y` = center, `h` spans up from feet (see `physicsStep` in `entities.js:3`).
- **Timing:** most updates use `1/60` per-frame constants (assumes 60fps) — match that in main.js (fixed-ish timestep or scale by `util.delta()`).
- **No comments unless needed** (existing code is lightly commented, mostly section headers).
- **No external libs.** Procedural everything.
- `Math.random()` is used freely at runtime (only world gen needs `mulberry32` determinism from seed).

---

## 6. Status

**Working / implemented:**
- **`js/main.js` exists** (~1,580 lines) and the game **runs**. Verified via headless-Chrome smoke tests: **24/24 checks PASS on the latest session** with **no console errors** — world gen (temple/bulbs/water/jungle), player swim/jump/mine, all 16 bosses + 6 event bosses spawn+step+render, 25 town NPCs + guide, minions & whips, Plantera bulb trigger, Moon Lord victory overlay, panels, minimap, hardmode gap-fill items/recipes/drops, hooks/mounts/fishing/pets/events/chests/dyes/ammo, pre-hardmode phase, selectable evil biome.
- Full post-Hardmode progression implemented: **10 bosses** (mechs → Queen Slime → Plantera → Golem → Duke Fishron → Empress → Lunatic Cultist → Moon Lord), each with AI, phase 2, drops, boss bars, and defeat tracking.
- World generation: new biomes (Jungle, Ocean, Temple), jungle/corrupt/hallow terrain, chlorophyte/titanium/orichalcum ores, Lihzahrd Temple (`world.templeCenter`), Plantera bulbs, water tiles — `world.js`.
- Full item + recipe system: ores/bars, weapons, tools, 6-piece armor sets, accessories (6-slot, `accEffects()`), whips (`e.whipped` minion marking), minion staves + `spawnMinion`, boss summons, souls/fragments — `items.js`, `crafting.js`.
- Player: swimming/water movement, triple-jump/wings, accessory effects (runSpeed/dmg/magic/mana/regen/invuln), whip + minion commands, melee/ranged/magic damage — `player.js`.
- Enemy AI for ~25 enemy types incl. hardmode, jungle, corrupt, hallow, temple (Lihzahrd), and lunar foes — `entities.js`.
- All 10 boss spawns/AI + `killBoss` drops + `game.onBossDefeated` hook — `bosses.js`.
- Full renderer: all new tiles (incl. WATER, PLANTERABULB, TEMPLEBRICK), enemies, bosses, minions, projectiles, whip/teleport FX, new minimap colors — `render.js`.
- Synthesized audio — `audio.js`.
- `main.js`: all 10 bosses routed in `spawnBoss`, minion stepping, `updateBulbs()` Plantera trigger, biome-aware `pickEnemy()` with hardmode enemies, accessory equip UI, `onBossDefeated`/`checkBossCompletion` victory overlay.

**Hardmode expansion session:**
- **Achievements system** — `js/achievements.js` (new): `ACHIEVEMENTS` map (39 entries: bosses, NPC arrivals, mining, progression, potions, summoning), `Achievements.unlock(id, game)`, `renderAchievements()`. New Achievements tab in `index.html` + `#panel-achievements` + script tag; `main.js` `switchPanel`/`renderPanel` include `'achievements'`. Unlock hooks: `game.onBossDefeated`, craft click (potion/mech items), `checkAchievements()`, player actions (`tryMine` ores/wood, `tryConsume` buff/heart, `tryMinion`, `tryWhip`). Unlocked state is session-only (not persisted).
- **8 friendly town NPCs** (MERCHANT, NURSE, WIZARD, STEAMPUNKER, CYBORG, TRUFFLE, PIRATE, WITCHDOCTOR) spawned by `spawnNpcs()` in main.js; `ENT_DEF` entries `hp:9999, dmg:0, def:99`; NPCs excluded from melee/whip/projectile/minion targeting and spawn caps via `e.dmg > 0` guards (`nearestEnemy`, `projHitEnemy`, `minionTarget`, `tryMelee`, `tryWhip`, spawn counter).
- **Vanilla Hardmode content additions** — new tiles Snow/Ice/Mushroom/Palladium (`const.js` T, `TILE_HARD`/`TILE_DROP`, `render.js` sprites + `TILE_COLORS`/`MINIMAP_COLORS`); new biomes Snow/Desert/Mushroom in `world.js` (surface bands + `mushroomAt` + palladium ore at depth>290); ~40 new enemies incl. biome/lunar foes + Archer/Golem/Tortoise/Ichor Sticker/Clinger/Mimic AI steps and drop tables (souls, fragments, LIFEFRUIT, TURTLESHELL, SHARKFIN); Wyvern drops soul quartet; new minion types `spider/pygmy/xeno/cell` in `spawnMinion`/`minionStep`; buff potions wired in `player.js` (buffs map, thorns reflect, invuln scaling); phase blades/sabers, hardmode melee/ranged/magic, whips, 9 armor sets, accessories, LIFEFRUIT, potions + full recipe set in `crafting.js`.
- **Fixed this expansion:** `buildGame()` called `wireGameMethods()` AFTER `spawnNpcs()`, so `Achievements.unlock` (which calls `game.message`) crashed on boot — reordered so methods are wired before guide/NPC/initial spawns. `tryMinion` passed the item id string instead of `def` to `spawnMinion` (broken summons) — fixed. Crafting panel now categorizes by item type incl. Summoner/Accessories.
- **Smoke test: 28/28 checks PASS, zero console errors** (world gen, 8 NPCs, guide, achievements panel/crafting/inventory panels, buff potion, LIFEFRUIT, minion spawn+step, whip fx, tile mining + dirty flag, all 10 bosses spawn+step, boss bars, minimap, mech-completion flag, 200-step clean loop). Throwaway `test.html` deleted. Test drives the game via direct `buildGame(seed)` + `step(1/60)` (RAF doesn't fire under `--virtual-time-budget`).

**Full-content expansion session (latest):**
- **All 25 town NPCs** — added 17 more to the original 8: DEMOLITIONIST, DYETRADER, ANGLER (w:16,h:22), ZOOLOGIST, DRYAD, PAINTER, GOLFER, ARMSDEALER, TAVERNKEEP (w:22,h:34), STYLIST, GOBLINTINKERER, CLOTHIER, MECHANIC, TAXCOLLECTOR, PARTYGIRL, SANTA, PRINCESS (`E` ids 100–116 in `const.js`). `ENT_DEF` `hp:9999, dmg:0, def:99, speed:0` + enemyStep no-op cases (`entities.js`), `drawHumanoid` render cases (`render.js`), all spawned + achievement-unlocked in `spawnNpcs()` (`main.js`) spread −1250..+1100 px around spawnX on the surface. 17 new arrival achievements (`achievements.js`, now 56 total). Combat/targeting exclusion unchanged via `e.dmg <= 0` guards.
- **49 new items + 40 new recipes** (`items.js`, `crafting.js`): materials BONE/SILK(craft 2 cobweb)/LEATHER/FEATHER/CRYSTALSHARD + 6 gems; 5 axes (Iron→Chlorophyte); True Excalibur / True Night's Edge; ammo (Musket/Silver/Explosive/Chlorophyte Bullets, Unholy/Jester's/Holy Arrows); Shotgun; Sky Fracture; 6 new minion staves (Abigail/Blade/Raven/Tempest/Desert Tiger/Terraprisma) + 2 new minion AI types (`abigail`, `blade` melee-chase) and sizes/attack cadence for `raven/tempest/tiger` in `spawnMinion`/`minionStep`; 3 whips (Spine/Cool/Dark Harvest); accessories Frog Leg, Angel Wings, Terraspark Boots, Hercules Beetle, Pygmy Necklace, Papyrus Scarab, Necromantic Scroll (all add `minion` accessory effect → +minion cap via `accEffects()`), Obsidian Rose; food (Cooked Fish, Pumpkin Pie) + Golden Apple (heal + buff now both apply in `tryConsume`) + Obsidian Skin / Water Walking potions.
- **New enemy drops** (`dropTable` in `entities.js`): skeletons→BONE, wolf→LEATHER, wyvern/giant bat→FEATHER, pixie/unicorn→CRYSTALSHARD, slime/pixie→gems, derpling/mosshornet→SILK. Count-0 drops are filtered by `game.addPickup`.
- **Smoke test: 21/21 checks PASS, zero console errors** (25 NPCs + guide spawn, 25 arrival achievements unlocked, all 49 new defs exist, recipe for every craftable new item, `recipeCategory` covers new types, minion-cap accessory, craft Silk x2 + potion, slime gem/pixie shard drops, all 6 new minions spawn+step, Golden Apple heal+buff, all 10 bosses spawn+step, crafting/achievements panels render, full `I`↔`ITEMS` consistency, 300-step clean loop, no console errors). Throwaway `test.html` deleted.

**Vanilla hardmode gap-fill session (latest):**
- **67 new items + 67 new recipes** (`items.js` `I` enum + defs, `crafting.js`): materials ICHOR, BROKENHEROSWORD, LUMINITE, LUMINITEBAR; tools Palladium/Orichalcum/Titanium Axes + Pwnhammer + Chlorophyte Jackhammer + Spectre Hamaxe; melee Night's Edge, Light Disc, Dao of Pow, Chain Guillotines, Paladin's Hammer, Possessed Hatchet, Flying Knife, Amarok, Gradient, Titanium Trident, Adamantite Glaive; ranged Clockwork AR, Boomstick, Marrow, Ice Bow, Xenopopper; ammo Cursed/Ichor/Venom Bullets + Frostburn/Hellfire/Bone Arrows; magic Nimbus Rod, Spirit Flame, Bubble Gun, Laser Machinegun, Nebula Blaze, Nebula Arcanum, Unholy Trident; summon staves Hornet/Sanguine/Deadly Sphere; 12 accessories (Lightning Boots, Avenger/Destroyer Emblems, Rifle/Sniper Scopes, Celestial Cuffs, Magic Quiver, Putrid Scent, Charm of Myths, Moon Charm, Neptune's Shell, Arctic Diving Gear); **10 new wings** (Harpy/Ice/Bone/Spooky/Fishron/Hoverboard + 4 Luminite wings, progressive `jumpMul` 1.0→1.3); food Baked Potato/Apple Pie/Burger; potions Inferno/Ammo Reservation; boss summon **Celestial Sigil** (`boss:'moonlord'`, craftable from fragments+souls).
- **Recipe fixes:** Night's Edge added as a 5-material recipe (4 swords + souls); True Excalibur now requires BROKENHEROSWORD; True Night's Edge re-chained to NIGHTSEDGE + BROKENHEROSWORD; Spectre Hamaxe no longer consumes the Spectre armor set.
- **Ammo wiring:** weapon defs updated to per-ammo types — Hallowed Repeater→Holy Arrow, Onyx Blaster→Cursed Bullet, Sniper Rifle→Ichor Bullet, Venus Magnum→Venom Bullet, Grenade/Proximity Mine Launchers→Explosive Bullet (other guns stay on base BULLET; `tryShoot` consumes the weapon's `ammo` id directly).
- **Drops wired** (`entities.js` `dropTable`, `bosses.js` `killBoss`): Ichor Sticker→ICHOR, Frankenstein/Reaper→BROKENHEROSWORD (10%), Moon Lord→LUMINITE (30–45). No Black Lens (no Demon Eye enemy exists).
- **3 new minion AI types** (`spawnMinion`/`minionStep`): `hornet`, `sanguine`, `sphere` — sizes + attack cadence + projectile type cases added to the generic fly-shoot path.
- **Smoke test: 59/59 checks PASS, zero console errors** (full I↔ITEMS consistency, 67 new defs exist with expected fields, weapon ammo refs, recipe for every new craftable item, Night's Edge/True weapon/Luminite Bar recipe chains, ichor/BHS/Luminite drops, Moon Lord LUMINITE pickup on `killBoss`, all 3 new minions spawn+step, all 10 bosses spawn+step, end-to-end crafts of Luminite Bar and Night's Edge, 300-frame clean step loop). Throwaway `test.html` deleted.

**"Extra systems" session (latest):**
- **Grappling hooks** — `type:'hook'` items (Grappling/Amethyst–Diamond/Ivy/Slime/Dual/Web/Candy Cane/Lunar/Spooky) with `hookRange/hookSpeed/hookPull/hookCount`; `Player.tryHook` casts to the clicked tile, `Player` pulls toward the anchor, rope line rendered; `hookCd` re-fire cooldown. Hook crafting chains (gem hooks → Ivy → Dual; Lunar from Lihzahrd bits; Spooky from spooky wood). Movement, jump, and fall-damage logic adjusted so a hooked player doesn't take fall damage.
- **Mounts** — `type:'mount'` items (Unicorn, Slime, Bee, U.F.O., Witch's Broom, Reindeer, Shrimpy Truffle) with `mount/mountSpeed/mountJump/mountFly/mountWater`; `tryMount` mounts/dismounts, movement speeds/flight/water buoyancy applied, mount body+legs drawn on the player. Craftable where applicable (Unicorn horn, etc.).
- **Fishing** — 4 rods (Wooden→Golden, `fishingPower` 15/25/35/50), 2 bait (Worm 20 / Nightcrawler 35), 10 fish + Zephyr Fish, 3 crates, recipes; `tryFish` casts toward water (`world.isWaterAt`), bobber waits `waitT` scaled by power, bite → reel to `rollFish()` (biome/depth-aware loot), crate consume via `openCrate(tier)`; fishing potion buff (+25 power). Cooked Fish recipe.
- **Pets & light pets** — `type:'pet'` (Zephyr Fish, Puppy, Baby Dino, Baby Eater) and `type:'lightpet'` (Wisp, Shadow Orb, Magic Lantern) with `pet:`/`light:` keys; `tryPet` toggles them; they follow the player, light pets add a light radius in `drawLighting`. Craftable (Puppy from silk; Magic Lantern from jungle materials).
- **Pumpkin Moon / Frost Moon / Martian Madness** — `type:'eventitem'` summons (Pumpkin Moon Medallion, Naughty Present, Martian Probe drop) used at night; event driver in `main.js` (`startEvent`/`updateEvents`/`startWave`/`spawnEventEnemy`/`endEvent`) with `EVENT_WAVES` (trash mobs + mini-bosses each wave), wave escalation, `#eventhud` "Event — Wave N of K" indicator; ambient spawning paused during events. **6 event bosses** (`spawnMourningWood`, `spawnPumpking`, `spawnEverscream`, `spawnSantank`, `spawnIceQueen`, `spawnMartianSaucer`) with AI, phase 2, drops (RazorPine, Snowman Cannon, The Horseman's Blade, Influx Waver, Celebration, Luminite) routed through `game.spawnBoss`; event boss bars removed on `endEvent`. Martian Probe spawns naturally near the player (underground trigger) and its death starts Martian Madness via `game.onEnemyKilled`.
- **Chests / storage** — `T.CHEST` tile, `world.chests` map + `chestAt`/`removeChest` (20-slot inventories); `Player.tryPlace` can place chests; right-click (`MOUSE.rightJust`) opens chest UI (`game.chestOpen`, `#panel-chest`, `renderChest`, click-to-transfer between inventory and chest); breaking a chest spills its contents as pickups (`world.spilledChestItems` consumed by `addPickup`).
- **Dyes** — 12 `type:'dye'` items (Red→Black + Rainbow), 6-slot `Inventory.dyes`, `equipDye(i)` swaps selected dye in; `mixColor()` blends base hex with dye tint; dye grid UI in the inventory panel; player arm tint applied in render.
- **Rockets & darts ammo** — 4 rockets (I–IV), 5 darts (Dart/Crystal/Cursed/Ichor/Venom), Grenade, `type:'ammo'`; new weapons consume them: Razorpine→BONE/DART, Snowman Cannon & Celebration→ROCKET4, Coin Gun→COIN, Charged Blaster→BULLET/PLASMA; `P.DART`/`P.FROSTBOLT`/`P.PLASMA` projectiles.
- **Event weapons/blocks/potions/furniture** — The Horseman's Blade (pumpkin blade waves), RazorPine, Snowman Cannon, Influx Waver, Charged Blaster, Celebration, Coin Gun, coin ammo; blocks/placeables: Glass, Spooky Wood, Honey (liquid), Chest, Chair, Table + glass/spooky-wood furniture sets; potions: Fishing, Inferno, Battle, Crate, Sonar, Heartreach; new drops: pumpkin, ectoplasm, Martian items.
- **Fixes this session:** `recipeCategory` restored after being dropped (now one definition extended for hook/fishingrod/ammo/mount/pet/lightpet/eventitem); orphaned duplicate `chestClick` body removed; all 9 edited files pass the ES5 syntax checker.
- **Smoke test: 24/24 checks PASS, zero console errors** (all new defs exist + I↔ITEMS consistency, recipe for every key new item, dyes array + jumps/noFall acc effects, hook attach, mount+dismount, fishing cast→bite→catch, crate loot, chestAt + chest-spill pickups, pumpkinmoon+frostmoon events start, event mini-boss spawns with boss bars, `endEvent` clears, all 6 event bosses spawn+step, dye equip + golden horseshoe noFall, all 10 original bosses, 300-frame clean loop, renderChest UI). Throwaway `test.html` deleted.

**Biome expansion session (latest):**
- **Crimson** — new surface biome in the far east (x 0.88–0.94W, replacing old eastern jungle tail): `T.CRIMGRASS`/`T.CRIMSTONE` surface + `T.CRIMTANE` ore veins (crimson deep stone swapped like corrupt/hallow). Spawns Crimera, Face Monster, Herpling, Blood Crawler, Crimson Axe, Ichor Sticker, Crimson Mimic via `pickEnemy`; crimson fishing (EBONKOI); crimson-tinted sky. `const.js` `BIOME.CRIMSON:12` + tiles 40–49 + `TILE_HARD`/`TILE_DROP`/`E` ids 150–162.
- **Sky Islands** — 8 floating cloud islands (`world.skyIslands` `{x,y,w}`) of `T.CLOUD` at surface−34..66 tiles, ~half spawn a sky chest (`world.chests`) with Cloud blocks, Golden Horseshoe, Starfury, diamond; `BIOME.SKY` when ≥28 tiles above surface; Harpy added to the sky spawn pool.
- **The Underworld / Hell** — `world.hellY` = row `0.82H`; terrain below is `T.ASH` with `T.HELLSTONE` + `T.OBSIDIAN` veins, `T.HELLBRICK` ruins, 16 lava pools + a bottom lava sea. `BIOME.UNDERWORLD` when `y > hellY`. Enemies: Lava Slime, Hellbat, Demon (fires cursed-flame bolts), Fire Imp (fireballs), Bone Serpent (wyvern segment AI). Drops: hellstone/obsidian/ash/hellbrick. `isLavaAt`/`liquidAt` added; lava is non-solid (`isSolidTile`).
- **Aether (Shimmer)** — `world.aetherPos` + a `T.SHIMMER` pool in the far-east caverns; `BIOME.AETHER` region; non-solid liquid; sky-blue minimap/sprite; themed spawns (Chaos Elemental, Pixie, Hallowed Mimic).
- **Underground mini-biomes** — spider caves (`spiderCols`, cobweb+stone pockets, Black Recluse/Blood Crawler), granite caves (`graniteCols`, Granite Golem), marble caves (`marbleCols`, Marble Golem); per-column `Uint8Array` markers drive `biomeAt` → `BIOME.SPIDER/GRANITE/MARBLE`; each has named HUD text ("Spider Cave" etc.).
- **Biome wiring** — `biomeAt` rewritten (aether→mushroom→hell→mini-biome→sky→surface bands); `pickEnemy`/`biomeName` extended for all new biomes; `drawBackground` surface sky now biome-tinted (crimson/corrupt dark green-red, hallow pink-blue, snow pale, jungle green); `render.js` TILE_COLORS + sprites + MINIMAP_COLORS + `drawEnemy` cases + new helpers (`drawHarpy`/`drawHellbat`/`drawCrimera`/`drawSpider`); `player.tileColor` map updated; `rollFish` adds crimson.
- **Items/recipes** — 10 new block items (crimstone, crimtane, ash, hellstone, hellbrick, cloud, granite, marble, obsidian) + Starfury sword (sky chests); Hellstone Brick recipe (furnace). All tiles mineable per `TILE_HARD` and drop their item via `TILE_DROP`.
- **Smoke test: 71/71 checks PASS, zero console errors** (world gen arrays, all 9 surface biome bands + SKY, ash/hellstone/lava/hellbrick/cloud/shimmer tile counts, UNDERWORLD/AETHER/SPIDER biomeAt, new tile solidity, 10 new item defs + all TILE_DROP refs, hellbrick recipe, 13 new enemies spawn+step, pickEnemy at aether/spider/crimson/hell/sky, biomeName, 300-frame clean loop, render at crimson/underworld/sky). Bone Serpent got its tail after this test: it spawns via `pickEnemy` in `updateSpawning`/`initialSpawns`, and those two spawn sites now call `initSegments(e, game, 5, '#d8c8a8')` (main.js:644,670) so the wyvern AI renders a 5-segment body. Throwaway `test.html` deleted.

**Pre-hardmode phase session (latest):**
- **Two-phase progression** — the game now starts in **pre-hardmode** and transitions to hardmode when the Wall of Flesh dies. `game.hardmode` flag (false by default) set in `buildGame`; `game.world.generate(game.hardmode)` gates ore veins: pre-hardmode gen places **copper/silver/gold/demonite** (depth tiers, demonite only in ebon/crim stone), hardmode gen keeps cobalt/mythril/adamantite/titanium/palladium/glowstone.
- **`game.startHardmode()`** (main.js) — set flag + message + roar, `world.startHardmode()` (injects hardmode ores + chlorophyte into the existing world, `postGenHp()`, `dirty=true`), kills the guide, re-runs `spawnNpcs()`. Triggered from `onBossDefeated('wallofflesh')`.
- **`spawnNpcs` is hm-gated** — pre-hardmode spawns 20 NPCs (ARMSDEALER −1250 … TAVERNKEEP +530); after hardmode appends STEAMPUNKER/CYBORG/TRUFFLE/SANTA/PRINCESS. Their arrival achievements unlock on the second `spawnNpcs` pass.
- **Pre-hardmode gear & items** (items.js + crafting.js) — 4 ores (COPPER/SILVER/GOLD/DEMONITE tiles 52–55), GEL/LENS materials, 5 bars (incl. CRIMTANEBAR), 4 pickaxes (Copper→Demonite), 4 swords, 3 bows, 5 armor sets (Copper/Silver/Gold/Demonite/Crimtane), 7 boss summons: `SLIMECROWN`→kingslime, `SUSPICIOUSLEYE`→eyeofcthulhu, `WORMLOOD`→eaterofworlds, `BLOODYSPINE`→brainofcthulhu, `ABEEMINATION`→queenbee, `CLOTHIERDOLL`→skeletron, `GUIDEVOODOODOLL`→wallofflesh. All gear has workbench/anvil/furnace recipes (3 ore:1 bar smelting); summons craft from materials (ebon/crim stone, honey, bone, silk, glowstone).
- **6 pre-hardmode bosses** (bosses.js `bossStep` dispatch + implementations; render.js `drawBoss` cases) — **King Slime** (bounce + teleport + slime split), **Eye of Cthulhu** (rush + phase-2 dash, spawns Servants), **Eater of Worlds** (18×300HP segments + 1800 head, `moveSegments` worm AI, shares destroyer's `bossHit` handling), **Brain of Cthulhu** (creepers + brain rush), **Queen Bee** (dash + stingers + bee swarms), **Skeletron** (head + `armType:'hand'` hands, contact damage; `armHit` message split skelprime vs skeletron), **Wall of Flesh** (dash + hungry minions; drops `I.PWHAMMER` + healing). All drop tables wired in `killBoss` (GEL/GOLD/LENS/DEMONITE/CRIMTANE/GLOWSTONE/HONEY/BONE).
- **Weak pre-hardmode enemies** (const.js ids 163–166, entities.js ENT_DEF + enemyStep + drawEnemy cases) — DEMONEYE (hovers toward player), CAVEBAT (erratic swoop), GOBLIN (scout walker), CORRUPTCRIMSONFLYER (dive bomber). **`dropTable(type, game)`** is now hardmode-aware: souls only drop when `game.hardmode`; SLIME now drops GEL; added DEVOURER/demon-eye/bat/goblin/flyer drops. `killEntity` passes `game`.
- **`pickEnemy` restructured** (main.js) — first branch `if (!game.hardmode)` returns weak pre-hardmode pools (slimes, zombies, demon eyes, cave bats, goblins, jungle/snow/corrupt/crimson critters, no hardmode/lunar enemies); hardmode pools unchanged below. `projectile` segment-hit generalized to `destroyer || eaterofworlds`.
- **7 new achievements** (achievements.js): kingslime, eyeofcthulhu, eaterofworlds, brainofcthulhu, queenbee, skeletron, wallofflesh.
- **Smoke test: 24/24 checks PASS, zero console errors** (hardmode starts false; copper/silver/gold/demonite generated and cobalt/mythril/adamantite/chlorophyte absent pre-hardmode; pickEnemy stable pre+post; 20 NPCs + guide pre-hardmode, no hardmode NPCs; all 7 pre-hardmode bosses spawn+step; Wall of Flesh defeat flips `game.hardmode`; startHardmode injects cobalt; hardmode NPCs spawn; hardmode twins still spawn; copper pick/bar/slime crown/guide voodoo doll recipes exist; 300-frame clean loop). Throwaway `test.html` deleted.

**Selectable evil biome session (latest):**
- **One evil biome per world, player-selectable** — the main menu now has an **Evil biome: Random / Corruption / Crimson** radio selector (index.html + style.css `.world-type`). `startNewGame()` reads `input[name="evil"]:checked` and passes it to `buildGame(seed, evil)` → `world.generate(hardmode, evil)` (world.js). `random` (or no arg) picks via `rng()` at gen time.
- **`world.evil`** = `'corrupt'` or `'crimson'` stored on the World. Both evil surface slots (west `f<0.30` and far-east `0.88–0.94`) become the chosen evil: `biomeAtX`/`biomeAt` return `BIOME.CORRUPT` or `BIOME.CRIMSON` accordingly — a corruption world has **zero** crimstone/crimtane/crimson-biome, and vice versa. All downstream consumers (deep-stone swap, surface grass, `pickEnemy` corrupt/crimson pools, `biomeName`, `rollFish` Ebonkoi, sky tint in render.js) already branch on the biome id, so they automatically match the chosen evil.
- **Evil-matched ore** — pre-hardmode gen now places **DEMONITE** only in EBONSTONE (corrupt world) and **CRIMTANE** only in CRIMSTONE (crimson world), so each world mines the matching ore for its bars/gear/summons.
- **Smoke test: 22/22 checks PASS, zero console errors** (corrupt world: ebonstone+demonite present, no crimstone/crimtane, west+far-east = CORRUPT, no CRIMSON biome anywhere, no crimson enemies spawn; crimson world mirrored; random + default resolve to corrupt|crimson; 300-frame clean loop; eaterofworlds + queenbee spawn+step in a crimson world). Throwaway `test.html` deleted.

**Fixed this session:**
- `main.js` boot crash: `game._last` written before the null check (loop now uses a module `lastNow`), and `wireGameMethods()` was called while `game` was still `null` (moved inside `buildGame`).
- `openPanel()` now calls `switchPanel()` so panel pages show/hide correctly; input state cleared on new game.
- **Day/night was globally inverted** in `render.js` (`night = t>0.22 && t<0.78`, and lighting got *darker at noon*). Fixed to `0 = midnight, 0.5 = noon`: sky/stars/sun-moon/lighting all corrected; `drawSunMoon` now arcs the sun across the day and moon across the night. `main.js` ambient-spawn night check matches.
- Chaos Elemental teleport fx missing `max` (invisible); `whip` fx had no draw case (now renders a lash line); new tiles missing from `MINIMAP_COLORS` (showed white) — all fixed in `render.js`/`entities.js`.

**Ores + biomes + structures session (latest):**
- **Dungeon structure** (`world.js`) — brick tower on the west edge (x ≈ 6–11% W), hollow interior with `T.DUNGEONBRICK`, a vertical entrance shaft sealed by a 2×2 `T.DUNGEONDOOR` gate (`world.dungeonDoors`), interior platforms, and 3 chests (Golden Key + Muramasa / Aqua Scepter / Cobalt Shield + gems). `world.openDungeon()` clears the gate tiles when **Skeletron** is defeated (`main.js` `onBossDefeated`). New `BIOME.DUNGEON` with named HUD text "The Dungeon".
- **Missing pre-hardmode ores** — added **Tin/Lead/Tungsten/Platinum** tiles (58–61) + bars + pickaxes/swords/armor sets; ore gen is now **alternate-tier** by depth: shallow Copper-or-Tin + Iron-or-Lead, mid Silver-or-Tungsten, deep Gold-or-Platinum (pre-hardmode), while hardmode gen now ALSO places the basic ores (so a hardmode world has everything) plus Cobalt/Mythril/Adamantite at reachable depths (fixed `depth>290` Adamantite threshold that could never generate — max depth is ~256; now >235). `startHardmode()` injection thresholds matched (110/170/235).
- **Meteorite** — `T.METEORITE` tile + bar + Meteor armor + Space Gun; 1–2 **meteor craters** carved at gen; `world.spawnMeteor()` opens a fresh crater (called on first Eater of Worlds / Brain of Cthulhu defeat). Meteorite also appears in hardmode gen.
- **Underground biomes** — `BIOME.UNDERDESERT` (deep sand columns under desert surface) + `BIOME.UNDERSNOW` (deep ice/snow under snow surface) with per-column `Uint8Array` markers; both skip mushroom-patch columns so `biomeAt` stays consistent. New enemies: **Antlion / Antlion Charger / Dungeon Scorpion** (desert), **Ice Slime** (snow), **Cursed Skull / Angry Bones / Dark Caster / Dungeon Slime** (Dungeon) — `ENT_DEF` + `enemyStep` + `dropTable` + `drawEnemy` cases. `pickEnemy` routes all three biomes in pre-HM + HM branches; `biomeName` returns "Underground Desert"/"Underground Snow"/"The Dungeon".
- **Structures** — **Desert pyramids** (1–2, SANDSTONE mound + buried chest: gold/platinum bars, gems, Pharaoh's mask), **living trees** (3–4 hollow trunks + base chest), **underground cabins** (6–9 wood rooms with chests), all with chest loot via `world.chests`.
- **Smoke test: 40/40 checks PASS, zero console errors** (dungeon bricks/doors/chest-with-Golden-Key, all 4 new ores, meteorite from gen + `spawnMeteor`, pyramid sandstone, desert+snow columns, dungeon/underdesert/undersnow `pickEnemy` + `biomeName`, new item defs + recipes, 8 new enemies spawn+step, `openDungeon`, pre-HM world has no hardmode ores, crimson world has crimstone/crimtane + no ebonstone/demonite, HM world has cobalt/mythril/adamantite + all basic ores + dungeon + meteorite, 300-frame clean loop). Throwaway `test.html` deleted.

**Critical progression repair session (latest):**
- **Boss damage fixed** — Destroyer/Eater head and segment damage now use `defV`; `makeBoss` also exposes `def`. Segmented bosses no longer receive `NaN` HP/damage.
- **Recipe integrity fixed** — corrected four invalid fish IDs (`FISH_CRIMSONTIGER`, `FISH_NEONTETRA`, `FISH_PUFFER`) and added obtainable Pumpkin Moon Medallion/Naughty Present recipes. Hoppin' Jack now drops Pumpkin and Reaper drops Ectoplasm so both event summons have non-event material sources.
- **Event driver repaired** — wave 1 now uses `sizes[0]`, only event-tagged enemies block progression, final waves call `endEvent`, zero-pending waves cannot spawn an extra enemy, and spawned trash/mini-bosses are tagged. Martian Madness can now finish normally.
- **Progression gates fixed** — Plantera bulbs require Hardmode plus all three mechanical bosses; the suppressed all-mechs completion path was restored; `spawnNpcs()` deduplicates existing town NPCs during Hardmode transition.
- **Missing combat/render wiring** — Devourer has a real definition/AI/render path; Martian Probe is targetable, appears in the Hardmode high-sky pool, and starts Martian Madness when killed in space; ordinary Pumpkin/Frost/Martian event enemies now render; Moon Lord death lasers render; Platform/Workbench/Furnace/Anvil/Plantera Bulb sprites and station minimap colors now exist.
- **Smoke test: 16/16 checks PASS, zero console errors** (all recipe materials resolve, both event summon recipes, station/bulb sprites, Devourer spawn+step, finite segmented-boss damage, Plantera pre-HM/pre-mech blocks + post-mech unlock, first/final event wave behavior, event tagging, NPC deduplication, event/death-laser rendering, 120-frame clean loop). All 5 edited JS files pass the ES5 checker; throwaway `test.html` deleted.

**Pre-Hardmode Underworld completion session (latest):**
- **Complete Hellstone progression** — Hellstone/Hellbrick are now mineable with 65-power Demonite or Meteorite picks; Cobalt now requires 70 power, making the new **Molten Pickaxe** the bridge into Hardmode. Added Hellstone Bars (3 Hellstone + 1 Obsidian at a Hellforge), Volcano, and a full Molten armor set with recipes.
- **Underworld houses** — world generation replaces the four empty Hellbrick outlines with 8 carved/furnished houses (`world.underworldHouses`), including tables, chairs, torches, 4 generated Hellforges, and 4 locked Shadow Chests. The Underworld now takes biome priority at `ty >= hellY` and has a dedicated red/black background.
- **Shadow Chest progression** — a guaranteed reusable Shadow Key appears in the first Dungeon chest. Locked Shadow Chests reject opening/mining without it, unlock through the chest UI, preserve their records/spilled contents, and contain all four unique loot weapons across the world: Sunfury, Flamelash, Hellwing Bow, and Dark Lance, plus Hellstone Bars and Obsidian Skin Potions. Chest-to-inventory transfer now preserves remainder when inventory is full.
- **Hellforge station** — new tile/item/sprite/minimap color; generated forges can be mined, placed, discovered by `findStations`, and used by crafting through the new `hellforge` station flag.
- **Voodoo Demon and Wall gate** — Voodoo Demon has a definition, Demon-style ranged AI, distinct doll-carrying render, Underworld spawn-pool wiring, and a guaranteed Guide Voodoo Doll drop. The old ordinary doll recipe was removed; the doll only summons Wall of Flesh while the player is in the Underworld.
- **Lava gameplay** — lava now slows/swims like a liquid and deals contact damage; an active Obsidian Skin Potion prevents that damage.
- **Smoke test: 25/25 checks PASS, zero console errors** (8 houses, 4 Hellforges, 4 locked Shadow Chests and all unique loot, guaranteed Shadow Key, `hellY` biome boundary, all new item definitions, mining gates, Hellforge detection, end-to-end Hellstone Bar + Molten Pickaxe crafting, Voodoo Demon drop/AI, key lock/unlock behavior, lava damage/immunity, Wall summon location gate, sprites/background/enemy rendering, 120-frame clean loop). All 8 edited JS files pass the syntax checker; throwaway `test.html` deleted.

**Natural evil + Jungle boss structures session (latest):**
- **Corruption/Crimson chasms** — every world now generates 3 deterministic vertical evil chasms (`world.evilChasms`) with side tunnels and 6 matching chambers. Corruption worlds contain only breakable Shadow Orbs; Crimson worlds contain only Crimson Hearts. The objects are non-solid, one-time world tiles and cannot be placed by players.
- **Natural evil-boss progression** — mining an Orb/Heart grants matching ore plus a rotating classic four-item loot pool. Corruption: Musket, Vilethorn, Ball O' Hurt, Band of Starpower. Crimson: The Undertaker, Crimson Rod, The Rotted Fork, Panic Necklace. Every third object summons the world's Eater of Worlds or Brain of Cthulhu; generated objects cannot be destroyed while another boss is active. Crafted Worm Food/Bloody Spine remain repeat summons.
- **Jungle Bee Hives** — 3 deterministic elliptical Hive-block structures (`world.beeHives`) generate in the underground Jungle, avoid the Temple/Plantera bulbs/each other, contain Honey deposits, and hold one breakable Larva each (`world.larvae`). Mining a Larva summons Queen Bee; boss-alive protection prevents wasting it. Abeemination remains the rematch summon.
- **Visual/content wiring** — new Shadow Orb/Crimson Heart/Larva/Hive tile sprites, minimap colors, mining particles, and solidity rules. Added `evilobject` ("Smashing, Poppet!") and `larva` ("Not the Bees!") achievements.
- **Smoke test: 22/22 checks PASS, zero console errors** (explicit corruption + crimson worlds, 3 chasms/6 matching objects per world, one-evil isolation, chamber air, 3 hives/3 larvae in Jungle, legacy Dungeon/Temple/Underworld structures preserved, 8 classic loot definitions, sprites, first/second/third break progression, Eater/Brain routing, pickup loot, boss-alive Larva guard, natural Queen Bee summon, achievements, normal render, 120-frame clean loop). All 7 edited JS files pass the syntax checker; throwaway `test.html` deleted.

**Celestial Pillars + invasions + Solar Eclipse + Altars session (latest):**
- **Lunar Pillars** — after the **Lunatic Cultist** dies, `game.onBossDefeated('cultist')` calls `game.startLunarPillars()` which spawns 4 themed pillars (Solar/Vortex/Nebula/Stardust via `spawnLunarPillar(game, type)`) at sky positions spread across the world (`game.lunarPillars`). Each pillar has `shieldHp 15000`; while shielded it is **invulnerable** (`bossHit` returns when `shieldHp>0`) and periodically spawns its themed guardian (Cori te/Vortexian/Nebula Floater/Stardust Cell, tagged `eventEnemy`). **Killing guardians near a pillar drains its shield** via the hook in `game.onEnemyKilled` (300 hp/kill, floating "Shield: N%" text, `shieldDown` + "The X Pillar shield has shattered!" message at 0). With the shield down the pillar fires a 3-shot themed bolt spray (CURSEDFLAME/LASER/PHANTOMBOLT/MAGICBOLT/CORITEBOLT) and takes reduced damage (`max(1, dmg - defV*0.5)`). Destroying a pillar drops 20–34 of its themed fragments + potions and unlocks `solar/vortex/nebula/stardustpillar` achievements; destroying all four unlocks `celestial` ("Celestial Clarity") plus a burst of colored FX. The **Celestial Sigil is gated**: `Player.trySummon` refuses while `game.pillarsSpawned && game.pillarsDestroyed < 4` ("The Moon Lord is sealed by the Celestial Pillars!"), and `anyBossAlive` blocks it while a pillar is still up.
- **Goblin Army** — summonable day-or-night with the **Goblin Battle Standard** (`TATTEREDCLOTH×10 + WOOD×5 + IRON×3` at an anvil; Tattered Cloth drops 60% from goblins); also triggers naturally at dawn (30% once `evilObjectsBroken >= 1`). Waves spawn GOBLINTHIEF/ARCHER/WARRIOR + the **Goblin Warlock** mini-boss (cursed-bolt caster, summons thieves, drops TATTEREDCLOTH).
- **Pirate Invasion** — summonable with the **Pirate Map** (`GOLD×15 + COIN×10 + SILK×5` at an anvil; also drops from pirates/captain); natural dawn trigger in hardmode (12%). Waves of PIRATEDECKHAND/CORSAIR/SHARK + **Pirate Captain** mini-boss (fireball cannon, summons flying sharks, drops a Pirate Map + gold).
- **Solar Eclipse** — daytime event; natural dawn trigger after all three mechanical bosses (20% while `game.mechDone`). Waves of FRANKENSTEIN/REAPER/SWAMPTHING/WEREWOLF/EYEBALL + the **Mothron** mini-boss (dive-swoop AI, phase 2 spawns baby EYEBALLs, drops 2 BROKENHEROSWORD). Ends at dusk (`updateEvents` checks `!day`).
- **`EVENT_WAVES`** extended with the three new events (each `day: true`); `startEvent`/`updateEvents` handle day-time events and the eclipse dusk cutoff; `endEvent` cleans up the new mini-boss bars/entities and unlocks `goblinarmy`/`pirateinvasion`/`solareclipse` achievements. `checkDawn()` (called from the main loop after the clock tick) drives the natural triggers on the night→day transition using `game._wasNight`.
- **`player.tryEvent`** now honors the item def's `time` field: `'any'` (Goblin Standard/Pirate Map) usable any time, `'day'` reserved for day events, default = night-only (Pumpkin/Frost unchanged).
- **Demon/Crimson Altars** (`T.ALTAR:70`, `TILE_HARD [60,60]`, solid, no `TILE_DROP`) — one altar per evil chasm chamber floor (`world.altars`, 3 per world, matching the chosen evil), tinted sprite + `TILE_COLORS`/`MINIMAP_COLORS`. **Only a hammer can smash them** (`tryMine` requires `def.hammer`, set on Pwnhammer/Chlorophyte Jackhammer/Spectre Hamaxe). Smashing (`onSpecialTileBroken` T.ALTAR) injects a vein of hardmode ore via new `World.prototype.spawnOreVein` (rotating Cobalt→Mythril→Adamantite tiers), increments `game.altarsSmashed`, floats "Altar smashed", unlocks `altar` ("A Land Made Flesh"), flashes + roar.
- **New enemies** (const/entities/render): GOBLINTHIEF/ARCHER/WARRIOR/WARLOCK (177–180), PIRATEDECKHAND/CORSAIR/SHARK/CAPTAIN (181–184), SWAMPTHING/WEREWOLF/EYEBALL/MOTHRON (185–188), each with ENT_DEF, enemy-step AI (archers shoot ARROW, warlocks MAGICBOLT, captains FIREBALL, sharks/eyeballs fly), drop tables, and explicit `drawEnemy` cases. New `drawBoss` cases for `lunar` (pillar column + rotating shield orb + shield % text), `mothron`, `goblinwarlock`, `piratecaptain`.
- **Smoke test: 41/41 checks PASS, zero console errors** (world + evil, altar gen/tile/solidity/no-drop in corrupt AND crimson worlds, crimson one-evil isolation, cultist→4 pillars + boss bars + all four themes, shield-break via guardian kills, pillar invulnerability while shielded, pillar kill → fragments pickup + achievements, all-four celestial + pillarsDestroyed, startLunarPillars idempotence, Celestial Sigil sealed before + allowed after pillars, goblinarmy/pirateinvasion/solareclipse start + wave progress, warlock/captain/mothron spawn + step, day-time Goblin Standard via tryEvent, altar smash ore injection + achievements, render + panels, Pwnhammer hammer flag, 300-frame clean loop). All 10 edited JS files pass the ES5 syntax checker; throwaway `test.html` deleted.

**Hardmode biome conversion/spread session (latest):**
- **Hardmode V-spread** — when the Wall of Flesh dies (`world.startHardmode()`), `World.prototype.carveVSpread()` carves the classic vanilla "V" underground: one diagonal arm of the world's evil (EBONSTONE or CRIMSTONE) and the other of Hallow (PEARLSTONE), running from ~55 tiles below surface at world center down to the Underworld ceiling (`hellY-4`). Only STONE/DIRT tiles convert; structures (Temple brick, Dungeon brick, wood cabins, hives, bulbs) and MUD (protects the Jungle) are untouched.
- **Slow creeping spread** — `World.prototype.seedSpreadFrontier()` scans every existing evil/Hallow tile with a convertible neighbor into `world.spreadFrontier`. Every `SPREAD_INTERVAL` (1.5s) game-time the main loop calls `World.prototype.spreadTick()`, which randomly converts up to `SPREAD_TICK_MAX` (4) neighboring STONE/DIRT/other-spreading-stone tiles per event, re-seeding the frontier with each converted tile (evil↔Hallow cross-convert, like vanilla; same-type neighbor conversions skipped to avoid churn). Spread never crosses `hellY`; `world.dirty` is set so the minimap rebuilds.
- **Underground biome detection** — `biomeAt` now scans a ±2 tile window around the player when below the surface; ebon/crim/pearl stone returns `BIOME.CORRUPT/CRIMSON/HALLOW` there. This makes spread real: HUD biome text (`biomeName` → "Underground Corruption/Hallow", etc.), `pickEnemy` pools (Corruptors/Wraiths in spread caverns, Pixies/Unicorns in hallowed ones), and `rollFish` all react to the creeping biomes. Mini-biome markers still take precedence.
- **Achievement** — new `spread` ("Cross Contamination", "Unleash Corruption and Hallow across the world.") unlocks in `game.startHardmode()` alongside a "Corruption and Hallow creep through the world!" message. All three edited JS files pass the ES5 checker.
- **Smoke test: 33/33 checks PASS, zero console errors** (hardmode starts false; pre-HM frontier empty + spreadTick no-op; V arms appear in the former forest band — ebon 568 + pearl 439 tiles — where no evil/Hallow existed; one-evil isolation (crimson world has no ebonstone); no spreading tiles below `hellY`; `biomeAt` returns CORRUPT/HALLOW/CRIMSON on V tiles; `pickEnemy` uses the corrupt pool in spread caverns; 400 direct `spreadTick`s grow the biome + set dirty + keep frontier alive; the main-loop timer drives growth across 300 `step(1/60)` frames; crimson-world V verified; render + panels clean). NOTE: the net spreading-tile count drops ~8k at `startHardmode` — that's the hardmode ore injection converting evil/Hallow stone to ores, expected. Throwaway `test.html` deleted.

**Town Pylon network session (latest):**
- **Nine biome Pylons** — Forest, Desert, Snow, Jungle, Hallow, Corruption, Crimson, Ocean, and Universal Pylon item definitions + recipes. All use the shared `T.PYLON:71` tile; each world record preserves the exact item identity through `world.pylons` so mining returns the correct Pylon. Universal links anywhere; biome Pylons link only when `world.biomeAt` matches their `pylonBiome`.
- **Placement and recovery** — `Player.tryPlace` accepts `type:'pylon'`, rejects placement in the Underworld, and registers successful placements with `World.prototype.addPylon`. `breakTile` removes the registry entry and exposes `spilledPylonItem`; `tryMine` restores that exact item directly to inventory. Pylons are hand-breakable (`TILE_HARD[T.PYLON] = [0,12]`) and have dedicated world/minimap colors and a glowing crystal sprite.
- **Teleport UI** — right-clicking a nearby Pylon opens the hidden `#panel-pylon` network page before chest interaction routing. A network requires at least two currently linked Pylons; the panel identifies the active node, valid destinations, wrong-biome nodes, and tile distance. Clicking a destination safely relocates the player above it, clears velocity/fall/hook state, plays teleport FX/audio, closes the panel, and unlocks the `pylon` ("Network") achievement.
- **Smoke test: 18/18 checks PASS, zero console errors** (tile hardness, all 9 item defs, all 9 recipes/material references, achievement definition, world registry initialization, Forest placement/linking, wrong-biome rejection, Underworld placement rejection, interaction prompt, panel states, delegated destination click, teleport + close + achievement, registry removal, exact-item recovery when mined, render, and 120 clean `step(1/60)` frames). All 8 edited JS files pass the ES5 syntax checker; throwaway `test.html` deleted.

**Player-selectable ammunition session (latest):**
- **Ammo families and fallback** — ammunition now belongs to `arrow`, `bullet`, `dart`, or `rocket` groups through `ammoGroup`; `ammoCompatible(required, candidate)` enforces family compatibility while preserving exact-match special fuel such as Razorpine's Bone ammo. `Inventory.ammo` stores the preferred item and `Inventory.prototype.ammoFor(required)` chooses it when compatible/in stock, otherwise falls back to the weapon default and then the first compatible inventory stack.
- **Combat wiring** — `Player.tryShoot` consumes the resolved ammo item, records it on the projectile, adds the ammo's damage to weapon damage, and reports "Out of compatible ammo!" only when no valid stack remains. Dart Rifle/Pistol now use Dart-family ammo and `P.DART`; Rocket/Grenade/Proximity launchers now use Rocket-family ammunition instead of bullets.
- **Inventory UI** — the inventory armor strip has an Ammo box. Select an ammo stack and click the box to prefer it; clicking it again toggles back to Automatic, and clicking with a non-ammo item selected also restores Automatic. Preferred stacks get a blue inset highlight, ranged item details show the currently resolved ammo, and ammo details show their family. The How To Play text documents the interaction.
- **Smoke test: 21/21 checks PASS, zero console errors** (all four family compatibility rules + cross-family rejection, exact Bone ammo, corrected Dart/Launcher defaults, all ranged ammo references, Ammo-box selection/highlight/change/automatic reset, ranged detail text, preferred consumption, projectile ammo/damage, empty and incompatible fallback, no-ammo firing block, render, and 120 clean `step(1/60)` frames). All 3 edited JS files pass the ES5 syntax checker; throwaway `test.html` deleted.

**Save system session (latest):**
- **Named IndexedDB worlds** — each world is stored as an independent IndexedDB record with its name, timestamps, evil biome, progression summary, and versioned snapshot. The old `tree.save.v1` localStorage slot migrates intact to a named `Legacy World` record and is removed only after the IndexedDB transaction commits. The mutable 1,024,000-tile world remains run-length encoded with `packTiles`/`unpackTiles`.
- **World and progression round trip** — saves chests and contents, exact Pylon identities, bulbs/evil objects/larvae/altars, meteor metadata, Dungeon state, pickups, time/hardmode/victory/kills, mechanical progression, evil-object/altar counters, events, achievements, and active Lunar Pillars (including shield/HP state). Ordinary enemies reset; non-Lunar active bosses block saving so a consumed summon cannot be lost.
- **Player round trip** — saves position, HP/mana/max values, buffs, mount, pets/light pets, all 50 inventory slots, selected slot, armor/accessories/dyes, preferred ammo, potion cooldown, and active minion types. Loading safely reconstructs runtime item references (`mountDef`, pet defs, minion staff defs) rather than serializing functions.
- **Menu and autosave flow** — the title lists every world with name, phase, evil, and last-save time; creation collects a name and evil; deletion requires confirmation. `Esc` exposes Resume and **Save & Quit**, while a safe one-minute timer and background-tab transition also autosave. Loading regenerates/restores behind the progress overlay and clears transient input/camera/panel state.
- **Account cloud sync** — signed-in players synchronize each world through `/_guard/saves` and `/_guard/save`; guests remain local-only. D1 stores one compressed world per row. Timestamp conflict resolution pulls newer copies, uploads newer local copies, and preserves deletion tombstones so removed worlds do not reappear on another device.
- **Verification:** browser lifecycle checks passed named creation, IndexedDB persistence, exact player-state reload, two-world listing, and deletion tombstones; a generated snapshot measured 620,206 bytes. The deployed API passed temporary-account authentication, world PUT/list/GET/delete, tombstone listing, and unauthenticated 401 checks. All JavaScript syntax and diff checks pass.

**Spawn-area balance session (latest):**
- **Hardmode town-safe zone** — `SPAWN_SAFE_X` (24 tiles) and `SPAWN_SAFE_Y` (14 tiles) define a surface-centered rectangle around `world.spawnX/spawnY`. `updateSpawning()` pauses only ordinary hardmode ambient spawning while the player remains inside it and keeps `spawnT` warm, so leaving town does not cause a queued instant spawn. Pre-hardmode weak spawning and all hardmode biome pools outside town are unchanged.
- **Safe respawns** — `game.respawn()` calls `clearSpawnHostiles()` in hardmode, removing only ordinary damaging enemies inside a slightly expanded town rectangle. Bosses, event/invasion enemies, player minions, town NPCs, and distant enemies are preserved. The ambient spawn cap now explicitly excludes minions, preventing summons from suppressing normal enemies while exploring.
- **Smoke test: 15/15 checks PASS, zero console errors** (safe-zone center/edges, pre-hardmode spawning unchanged, hardmode suppression inside town, hardmode spawning outside town, event ownership, minion spawn-cap exclusion, respawn position/vitals, nearby ordinary cleanup, distant/event/minion/boss preservation, 100 valid hardmode pool selections, render, and 120 clean `step(1/60)` frames). `js/main.js` passes the ES5 syntax checker; throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 1 - progression bridge (latest):**
- **Golem progression repaired** — Lihzahrd Power Cells are no longer circularly crafted from Temple Brick. Every generated Temple contains a chest with 3 Cells, and Lihzahrds/Flying Snakes can drop more. `Player.trySummon` now requires Hardmode, Plantera defeated, and the Temple biome before consuming a Cell.
- **Post-Plantera Dungeon roster** — added Paladin, Tactical Skeleton, Skeleton Sniper, Skeleton Commando, Ragged Caster, Necromancer, Diabolist, Bone Lee, Giant Cursed Skull, and Dungeon Spirit (`E` 189-198), with dedicated stats, ranged/ghost/melee AI, rendering, biome spawning gated behind Plantera, Ectoplasm acquisition, and canonical weapon/accessory drops.
- **Dungeon items made meaningful** — added Paladin's Shield, Tabi, and Black Belt. Paladin's Hammer, Tactical Shotgun, Sniper Rifle, Rifle Scope, Rocket Launcher, Spectre Staff, Shadowbeam Staff, Inferno Fork, Morning Star, and related rewards now come from Dungeon enemies instead of direct recipes. Master Ninja Gear now combines Tabi + Black Belt.
- **Organic Cultist trigger** — world generation stores `dungeonEntrance`; Golem's defeat schedules four ritual enemies (Lunatic Devotees + Cultist Archers, `E` 199-200). Killing the final ritual Cultist automatically summons the Lunatic Cultist, whose existing defeat hook starts the four Celestial Pillars. Ritual readiness persists through saves and old post-Golem saves recover it automatically.
- **Phase integrity fixes** — ambient Lunar enemies/fragments are sealed until Celestial Pillars are active. Obvious Hardmode enemies (Corruptors, Herplings, Crimson Axes, Hallow enemies, Mummies, Moss Hornets, Wolves, Temple enemies) were removed from pre-Hardmode ambient pools.
- **Parity roadmap** — new `PARITY.md` tracks progression integrity, Hardmode encounters, Blood Moon/Slime Rain/Deerclops/Torch God, Old One's Army, weather/Frost Legion, event roster expansion, and long-term enemy/item/acquisition parity.
- **Smoke test: 21/21 checks PASS, zero console errors** (12 enemy IDs/definitions, 3 accessories, Master Ninja recipe, canonical recipe removals, guaranteed + enemy-dropped Power Cells, Golem pre-Plantera/outside-Temple gates, pre/post-Plantera Dungeon pools, canonical drops, Dungeon Spirit spawning, all new enemy AI/render paths, 4-member ritual, Cultist activation, Lunar pre-Pillar seal + active-Pillar enable, save field, render, and 120 clean `step(1/60)` frames). All 8 edited JS files pass the ES5 syntax checker; throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 2 - progression integrity (latest):**
- **Twins fixed** — killing the first eye now removes only that eye and its boss bar; no completion flag or rewards are granted until the sibling dies. The second death grants one Hallowed Bar/Soul of Sight reward set and completes the mechanical gate exactly once.
- **Evil-boss mining bridge** — added Shadow Scales and Tissue Samples to Eater/Brain rewards, renamed the existing saved `demonitepick` item to Nightmare Pickaxe, added Deathbringer Pickaxe, and changed both 65-power recipes to require their matching boss material. Corruption and Crimson now have symmetric, boss-gated Hellstone progression.
- **Meteorite and Hallow phase gates** — new worlds generate with no Meteorite; the first Eater/Brain defeat triggers the existing one-time `spawnMeteor()` path. Pre-Hardmode no longer generates or reports the fixed Hallow band; `World.startHardmode()` activates/converts that surface band and the V-spread. `world.hardmode` is restored by saves.
- **Recipe progression metadata** — recipe registration infers `hm` and `after:'mechs'|'plantera'|'moonlord'` from progression materials. `recipeProgressionMet()` now gates crafting and the crafting panel hides recipes before their phase, while station/material checks remain unchanged.
- **Missing sources supplied** — Hornets drop Vine, Black Recluses drop Spider Silk, Skeleton Archers and Martian enemies drop Illegal Gun Parts, and a Hardmode anvil recipe provides a shop-system substitute for Illegal Gun Parts.
- **Boss summon restrictions** — Eye/Skeletron/mechs require night; mechs require Hardmode; Eater/Brain require the matching world evil and biome; Queen Bee requires Jungle; Queen Slime and Empress require Hardmode Hallow; Duke requires Hardmode Ocean; Golem retains Plantera/Temple gates; Moon Lord now requires that all four Pillars actually spawned and were destroyed.
- **Smoke test: 33/33 checks PASS, zero console errors** (new items/recipes, Shadow Scale + Tissue drops, no pre-boss Meteorite, one-time Meteor, first/second Twin behavior, pre-HM Hallow absence + Hardmode activation, all three missing material sources, inferred Hardmode/mech/Plantera/Moon Lord recipe gates, blocked summon cases + valid mech summon consumption, save-restored `world.hardmode`, render, and 120 clean `step(1/60)` frames). All 7 edited JS files pass the ES5 syntax checker; throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 3 - Hardmode encounters (latest):**
- **Biome Mimic encounters** - added Keys of Light/Night from 15 Souls, empty-Chest transformation in Hardmode, world-evil routing for the Key of Night, stronger Mimic stats and ranged attacks, and canonical Hallowed/Corrupt/Crimson reward pools. Crimson Mimics now also appear naturally in deep Crimson.
- **Goblin and Pirate rewards** - Goblin Warlocks now guarantee one Shadowflame weapon. Pirate Captains award Cutlass/Gold Ring loot, and the new final-wave Flying Dutchman ship bombards the player, deploys pirates, and drops Pirate Staff/Lucky Coin/Discount Card/Gold Ring rewards plus a Cutlass.
- **Progressive Solar Eclipse** - the base roster now includes Frankenstein, Swamp Thing, Creature from the Deep, and Fritz; post-mechanical progression adds Reaper/Werewolf/Eyeball; post-Plantera adds Possessed, Butcher, Deadly Sphere, Dr. Man Fly, Nailhead, Psycho, Baby Mothron, and Mothron. Broken Hero Swords were moved exclusively to the post-Plantera Mothron path.
- **Solar Tablets** - Lihzahrds and Flying Snakes drop Solar Tablet Fragments; eight fragments craft a Solar Tablet. Tablet crafting/use is Plantera-gated and starts a daytime Solar Eclipse.
- **Acquisition integrity** - removed direct recipes for Mimic, Eclipse, and Pirate enemy rewards. Added Pirate minion behavior and removed four unused, undefined pillar-fragment enum entries in favor of the existing functional Lunar fragment IDs.
- **Smoke test: 25/25 checks PASS, zero browser errors** (new item/enemy definitions, item enum and recipe material integrity, canonical recipe removals, empty-Chest Key transformation, evil-world routing, Mimic loot/attacks, tiered Eclipse pools, all new enemy AI/render paths, Warlock/Dutchman rewards, Pirate event wiring, Solar Tablet Plantera gate/use, and 120 clean `step(1/60)` frames). All 8 edited JS files pass the ES5 syntax checker; `git diff --check` passes; throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 4 - early events and bosses (latest):**
- **Blood Moon** - added natural dusk activation, Bloody Tear manual activation, dawn completion, red night skies, event-specific HUD, Blood Zombie/Drippler surface enemies, and Wandering Eye Fish/Zombie Merman/Dreadnautilus fishing encounters. Blood Moon enemies supply Bloody Tears, Shark Tooth Necklace, Chum Caster, and the formerly recipe-only Sanguine Staff.
- **Slime Rain** - added natural dawn activation and a dedicated kill-count event mode. Only event-tagged Green/Pink Slimes count; 50 kills on the first clear or 25 on repeats summons King Slime, whose defeat ends the rain and unlocks the event achievement. Event progress remains covered by the existing generic event save field.
- **Deerclops** - added Flinx Fur acquisition, evil-alternative Deer Thing recipes, Snow/Underground Snow summon gating, grounded jump AI, ice-spike/falling-ice/shadow-hand attacks, procedural rendering, and a reward pool containing Lucy the Axe, Pew-matic Horn, Weather Pain, Eye Bone, and Flinx Fur.
- **Torch God** - placing the 100th nearby underground Torch starts a standalone challenge that fires each recorded Torch at the player. Survival awards Torch God's Favor; consuming it permanently enables biome-colored Torch flames. Favor persists through saves, active challenges block saving, and the HUD reports challenge progress.
- **Event framework improvements** - added timed and kill-count event modes, surface-only event placement, natural dusk detection, metadata-driven HUD names for all eight events/invasions, and four new achievements (Blood Moon, Slime Rain, Deerclops, Torch God).
- **Smoke test: 38/38 checks PASS, zero browser errors** (item/enemy/achievement definitions, recipe integrity, Blood Moon summon/surface spawns/fishing/drops/AI/render/dawn completion/natural trigger/HUD, Slime Rain natural trigger/tagged kill counting/King Slime completion/HUD, Flinx Fur, Deer Thing biome gate, Deerclops attacks/render/rewards, canonical 100-Torch challenge/save block/Favor award-consume-save-load/render, and 120 clean `step(1/60)` frames). All 9 edited JS files pass the ES5 syntax checker; throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 5 - Old One's Army (latest):**
- **Eternia arena flow** - added a placeable/mineable Eternia Crystal Stand, right-click Crystal activation, evil-boss progression gate, strict 30-tile flat/clear lane validation on both sides, fixed animated portals, a rendered crystal objective with tier-scaled HP, objective HUD, active-Stand mining protection, and no Crystal consumption on invalid arenas.
- **Three progression tiers** - Tier 1 runs before the mechanical trio, Tier 2 after all mechs, and Tier 3 after Golem. Dedicated objective waves spawn only at the fixed portals and end in Dark Mage, Ogre, or Betsy respectively. Crystal destruction fails and cleans the event; victories persist per tier and award 5/15/30 Defender Medals.
- **Etherian roster and combat** - added Etherian Goblin, Goblin Bomber, Javelin Thrower, Kobold, Kobold Glider, Wither Beast, Drakin, and Etherian Wyvern with objective-focused AI. Kills grant transient Etherian Mana; event enemies, projectiles, bosses, sentries, and portals are isolated from ordinary invasion logic and cleaned at event end.
- **Four sentries** - Ballista, Flameburst, Explosive Trap, and Lightning Aura staffs consume event Mana rather than the item, use distinct ranged/trap/aura behaviors, target only Army enemies, scale from accessory damage, and are removed after victory/failure.
- **Tavernkeep progression** - right-clicking the existing Tavernkeep opens a dedicated shop. After an evil boss, a free Defender Kit supplies the first Stand, Crystals, and Ballista; Gold purchases replacement Stand/Crystals, Defender Medals purchase the other sentries, Squire armor, and tier-gated weapons. Dark Mage/Ogre/Betsy also drop Brand of the Inferno, Sleepy Octopod, and Betsy weapon rewards without polluting permanent boss flags.
- **Persistence and safety** - tier wins and the one-time Defender Kit persist through saves. Save & Quit is blocked during an active Army because transient waves, Mana, sentries, and objective state intentionally do not persist. Added four achievements and documented the interaction in How To Play.
- **Smoke test: 44/44 checks PASS, zero browser errors** (all definitions/IDs/tile colors, arena pass/fail and non-consumption, tier selection, activation/save block/HUD, active-Stand protection, fixed portals, objective-focused AI/damage/failure, Mana, all four sentries, all eight enemy paths, portal/crystal/enemy/boss rendering, all three bosses/reward isolation, controller-driven final-wave completion, tier wins/medals/achievements, cleanup, Tavernkeep gift/shop/tier stock, progress snapshot, and 120 clean `step(1/60)` frames). All JS files pass the ES5 syntax checker; `git diff --check` passes; throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 6 - secondary events and weather (latest):**
- **Frost Legion** - added a Hardmode Snow Globe summon, Snowman Gangsta/Mister Stabby/Snow Balla waves, persistent completion, event achievement, and Santa arrival gated behind a successful invasion clear.
- **Persistent weather** - added randomized rain storms whose biome-local presentation becomes rain, blizzard, or sandstorm. Storm duration, wind, and intensity drive HUD text, darker lighting, canvas particles, player wind force, and surface-only weather enemy pools; weather state round-trips through saves.
- **Weather encounters** - added Flying Fish and Angry Nimbus for rain, Angry Tumbler and Sand Elemental for sandstorms, existing snow enemies for blizzards, and obtainable Forbidden Fragments from Sand Elementals.
- **Expanded invasions and moons** - Pirate Invasions gained Pirate Crossbowers and Parrots; Martian Madness gained Walkers, Drones, and Tesla Turrets; Pumpkin Moon gained Hellhounds, Poltergeists, and Headless Horsemen; Frost Moon gained Zombie Elves, Elf Archers, Krampus, and Elf Copters. Matching event-completion achievements were added.
- **Expanded Lunar roster** - Solar/Vortex/Nebula/Stardust Pillars now each alternate between two themed guardians using Selenian, Storm Diver, Predictor, and Stargazer additions; all four drain pillar shields and drop their matching fragments.
- **Reward integrity** - Chain Gun now comes from Santa-NK1; Xenopopper, Laser Machinegun, and Xeno Staff now come from Martian Saucer rewards. Their direct recipes, along with direct Influx Waver and Charged Blaster recipes, were removed.
- **Smoke test: 41/41 checks PASS, zero browser errors** (23 enemy definitions, item/recipe/achievement integrity, Hardmode Snow Globe gate, controller-driven Frost Legion completion and Santa arrival, rain/blizzard/sandstorm selection/rendering/spawn pools/wind, weather save restoration, all expanded event AI/render/drop paths, Lunar guardian shield credit, canonical boss reward sources, event achievements, and 120 clean `step(1/60)` frames). All Batch 6 JS files pass the ES5 syntax checker; throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 7 - invasion roster integrity (latest):**
- **Goblin Scout bootstrap** - added rare natural surface Goblin Scouts with guaranteed Tattered Cloth, so Goblin Battle Standards no longer depend on materials sourced from the invasion they summon. Ordinary invasion goblins no longer drop the cloth.
- **Canonical Goblin Army** - added Goblin Peons and projectile-casting Goblin Sorcerers to every army. Hardmode armies add flying Goblin Summoners with Shadowflame attacks and a guaranteed Bow/Knife/Hex Doll reward; the non-vanilla Goblin Warlock miniboss is no longer scheduled.
- **Pirate roster** - added gun-firing Pirate Deadeyes to invasion waves. Pirate Captains now deploy Parrots instead of the non-vanilla Pirate Shark path.
- **Coin Gun acquisition** - removed the direct Coin Gun recipe, added a rare ordinary-Pirate drop, included it in Flying Dutchman rewards, and corrected item acquisition descriptions.
- **Smoke test: 23/23 checks PASS, zero browser errors** (five enemy definitions, item/recipe integrity, Scout cloth source, invasion-goblin cloth removal, pre-Hardmode/Hardmode Goblin pool split, no Warlock miniboss scheduling, Pirate Deadeye roster, all new AI/render paths, Parrot deployment, ordinary-Pirate and Flying Dutchman Coin Gun sources, and 120 clean `step(1/60)` frames). All seven edited JS files pass the ES5 syntax checker; throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 8 - moon roster and reward integrity (latest):**
- **Complete Frost Moon roster** - added Nutcracker, Flocko, Yeti, and Present Mimic to the existing Zombie Elf/Gingerbread/Elf Archer/Elf Copter/Krampus waves. Each has dedicated stats, AI dispatch, drops, and procedural rendering; Present Mimic uses the existing wake-and-lunge Mimic behavior with a gift-box visual.
- **Pumpkin Moon acquisition** - removed direct recipes for Necromantic Scroll, Spooky Hook, The Horseman's Blade, Raven Staff, and Dark Harvest. Mourning Wood now rolls Scroll/Hook, while Pumpking rolls Blade/Staff/Whip.
- **Frost Moon acquisition** - removed direct recipes for Razorpine, Christmas Hook, Chain Gun, Elf Melter, Snowman Cannon, North Pole, Blizzard Staff, and Reindeer Bells where applicable. Everscream, Santa-NK1, and Ice Queen now each roll from their canonical simplified reward pools; source descriptions were corrected.
- **Smoke test: 21/21 checks PASS, zero browser errors** (four enemy definitions, complete Frost Moon roster, non-canonical Frost Zombie exclusion, item/recipe integrity, all four AI/render/drop paths, Present Mimic activation, every possible principal reward across all five Pumpkin/Frost Moon bosses, source descriptions, Frost Moon completion, and 120 clean `step(1/60)` frames). All seven edited JS files pass the ES5 syntax checker; throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 9 - Blood Moon roster and fishing (latest):**
- **Surface roster** - added The Bride and The Groom to all Blood Moons, plus Hardmode-only Clowns with explosive projectile attacks. The timed event now uses the shared progression-aware event pool instead of bypassing it.
- **Hardmode fishing encounters** - added Blood Eel and Hemogoblin Shark to the existing Blood Moon reel path while preserving Dreadnautilus, Wandering Eye Fish, and Zombie Merman. Blood Eels initialize a six-segment body when hooked and use the existing worm pursuit system.
- **Weapon acquisition** - added Bananarang and KO Cannon to Clown drops, Haemorrhaxe and Drippler Crippler to Blood Eels, and Haemorrhaxe/Blood Thorn to Hemogoblin Sharks. All five are drop-only items with source descriptions.
- **Visual and combat wiring** - all five enemies have dedicated definitions, AI dispatch, drop routes, and procedural rendering; Clown attacks and both fishing-monster movement paths are active.
- **Smoke test: 23/23 checks PASS, zero browser errors** (five enemy and five weapon definitions, item/recipe integrity, Bride/Groom roster, pre-Hardmode/Hardmode Clown split, both fishing encounter pools, timed Hardmode spawning, actual reel-to-segmented-Eel/Shark spawning, all AI/render paths, every weapon source, dawn completion, and 120 clean `step(1/60)` frames). All six edited JS files pass the ES5 syntax checker; throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 10 - Martian Madness integrity (latest):**
- **Post-Golem trigger** - natural Martian Probes now enter the space pool only after Golem, and killing a Probe cannot start the invasion before that gate. The existing simplified kill-to-trigger interaction remains.
- **Expanded roster** - added Martian Officer, Martian Engineer, Gigazapper, Brain Scrambler, and Scutlix Gunner to every invasion. Engineers deploy event-owned Tesla Turrets; the four ranged elites use distinct laser/plasma/psychic attacks.
- **Signature rewards** - added Laser Drill and Anti-Gravity Hook to Engineer drops, Charged Blaster Cannon to Gigazappers, Brain Scrambler mount to Scutlix Gunners, and Electrosphere Launcher to the Saucer pool. All are drop-only and have corrected source descriptions.
- **Reward integrity** - Martian enemies and Probes no longer leak Vortex Fragments before the Lunar Pillars. Martian Saucer now rolls exactly one principal reward from Influx Waver, Xenopopper, Laser Machinegun, Xeno Staff, Cosmic Car Key, or Electrosphere Launcher instead of guaranteeing a multi-item bundle.
- **Smoke test: 30/30 checks PASS, zero browser errors** (five enemy/four item definitions, item/recipe integrity, pre/post-Golem natural pool and kill trigger, no Lunar fragment leakage, all new AI/projectile/render paths, Engineer turret ownership, every ordinary reward route, all six one-at-a-time Saucer outcomes, source descriptions, Martian completion, and 120 clean `step(1/60)` frames). All six edited JS files pass the ES5 syntax checker; throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 11 - Old One's Army integrity (latest):**
- **Tier progression** - Tier 2 now unlocks after Twins, Destroyer, or Skeletron Prime individually rather than waiting for the all-mechanical `mechDone` gate. Golem still takes precedence and unlocks Tier 3.
- **Missing Etherian enemies** - added Old One's Skeleton to Tier 1 waves and Dark Mage summons. Added flying, lightning-casting Etherian Lightning Bugs to late Tier 3 waves; both use objective-focused Army AI, transient Mana credit, and dedicated rendering.
- **Boss reward pools** - Dark Mage now rolls Apprentice's Scarf or Squire's Shield. Ogre rolls Brand of the Inferno, Sleepy Octopod, Phantom Phoenix, or Tome of Infinite Wisdom. Betsy rolls Betsy's Wrath, Aerial Bane, Flying Dragon, or Sky Dragon's Fury.
- **Acquisition integrity** - all ten boss rewards are drop-only; the old Brand/Octopod/Wrath/Aerial Bane medal-shop entries were removed while sentries and Squire armor remain purchasable.
- **Smoke test: 26/26 checks PASS, zero browser errors** (two enemy/six item definitions, item/recipe integrity, each individual mechanical gate, Golem Tier 3 precedence, tier-specific wave placement, Skeleton objective AI, Lightning Bug flight/projectiles, both render paths, Dark Mage Skeleton summon, every reward outcome across all three bosses, shop exclusion, source descriptions, Tier 2 victory/medals, and 120 clean `step(1/60)` frames). `js/oldonesarmy.js` passes the ES5 syntax checker; throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 12 - Lunar Pillar elites (latest):**
- **Solar encounter** - added eight-segment Crawltipedes that circle grounded players but aggressively pursue airborne targets; pillar spawning initializes and renders the full body.
- **Vortex encounter** - added Alien Queens with laser attacks and capped Alien Hornet summons. Both Queen and summoned Hornets belong to the Vortex shield-credit pool.
- **Nebula encounter** - added grounded Evolution Beasts that lead moving players with three-shot predictive Phantom Bolt volleys.
- **Stardust encounter** - added ghostly Flow Invaders with five-star projectile bursts; death releases two event-owned Stardust Cells that can continue draining the pillar shield.
- **Integration** - all four elites drop two matching fragments, count toward only their matching shield, render procedurally, and rejoin reconstructed guardian arrays when active Pillars are restored from a save.
- **Smoke test: 22/22 checks PASS, zero browser errors** (four enemy definitions, item/recipe integrity, per-pillar pool ownership, summoned-creature shield credit, segmented Crawltipede spawning and ground/air behavior, Queen summons/lasers, Evolution predictive volley, Flow star burst/death Cells, matching fragments, all four shield-credit paths, rendering, save restoration, and 120 clean `step(1/60)` frames). All five edited JS files pass the ES5 syntax checker; throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 13 - Solar Eclipse progression and rewards (latest):**
- **Eligibility repair** - natural Solar Eclipses now become eligible after Twins, Destroyer, or Skeletron Prime individually rather than waiting for the all-mechanical `mechDone` gate. Solar Tablets remain post-Plantera daytime summons.
- **Staged roster** - the first-mech roster adds Eyezor, Vampire, and The Possessed to Frankenstein/Swamp Thing/Creature from the Deep/Fritz. Reaper joins after all three mechs; Butcher, Deadly Sphere, Dr. Man Fly, Nailhead, Psycho, Baby Mothron, and Mothron remain post-Plantera. Werewolf was removed from Eclipse routing, and Vampire no longer appears during ordinary nights.
- **Signature acquisition** - added Eye Spring pet drops to Eyezor, Butcher's Chainsaw to Butchers, and Mothron Wings to Mothron. The Eye of Cthulhu yoyo recipe was removed and it now shares Mothron's principal reward roll; Broken Hero Swords remain guaranteed in the simplified progression.
- **Smoke test: 25/25 checks PASS, zero browser errors** (three item definitions, item/recipe integrity, pre-mech/first-mech/all-mechs/post-Plantera pools, Mothron gate, Werewolf exclusion, natural any-mech activation and pre-mech block, ordinary Vampire seal, Eyezor/Butcher drops, naming, both Mothron outcomes plus Broken Hero Swords, source descriptions, roster rendering, dusk completion, and 120 clean `step(1/60)` frames). All five edited JS files pass the ES5 syntax checker; throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 14 - core town NPC services (latest):**
- **Shared interaction panel** - added a hidden contextual Town page and cursor-targeted right-click routing for Merchant, Arms Dealer, Nurse, and Goblin Tinkerer. Eternia Stand and Tavernkeep retain priority, and the Tavernkeep's tiered Defender shop remains isolated.
- **Merchant and Arms Dealer** - added additive stock using the project's established Gold Ore currency. Merchant sells building supplies, basic ammunition, potions, fishing gear, bait, and post-evil-boss Mining Potions. Arms Dealer sells Musket Balls plus Skeletron/evil-boss/Hardmode-gated ammunition, Shotgun, and Illegal Gun Parts. Purchases are all-or-nothing and never charge a full inventory.
- **Nurse** - added full treatment priced at one Gold Ore per 50 missing health, without consuming potions or changing potion cooldown.
- **Goblin Tinkerer** - added five replaceable prefixes from Broken to Legendary for unstackable melee, ranged, magic, and whip items. Prefixes are stored on individual inventory slots, affect actual combat damage, display in item details, and never mutate shared `ITEMS` definitions.
- **Metadata integrity** - added stack-copy/add helpers so reforges persist through chest storage, dropped pickups, collection, and the existing save format. Old slots without reforge metadata remain valid.
- **Smoke test: 27/27 checks PASS, zero browser errors** (panel/NPC availability, contextual Merchant opening/close cleanup, purchase success/insufficient funds/full-inventory safety, Arms Dealer progression gates, Nurse cost/cooldown behavior, Goblin cost/prefix/global-definition isolation, melee/ranged/magic/whip damage application, metadata transfer/drop/pickup/save restoration, Tavernkeep independence, render, and 120 clean `step(1/60)` frames). All three edited JS files pass the ES5 syntax checker; throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 15 - expanded town NPC services:**
- **Four additional services** - Demolitionist, Dryad, Mechanic, and Wizard now use the shared contextual Town page. Their progression-aware shops cover explosives, terrain supplies and Pylons, building/storage supplies, mana supplies, and magic crafting components.
- **Dryad functionality** - added Purification Powder, local evil/Hallow cleansing, and a world-status report showing evil, Hallow, and purity percentages.
- **Magic acquisition** - added purchasable Spell Tome and Harp components to support existing magic weapon recipe chains.
- **Service hardening** - purchases and actions revalidate NPC proximity, unknown stock gates fail closed, contextual state resets cleanly, reforges never repeat the current prefix, and loaded reforge metadata is restricted to canonical prefixes.
- **Smoke test: 28/28 checks PASS, zero browser errors** (new item definitions and recipes, all four NPC panels/stocks, progression gates, purchase safety and range checks, Dryad status/purification, magic acquisition, reforge validation, panel cleanup, render, and 120 clean `step(1/60)` frames). All four edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 16 - persistent town NPC arrivals (latest):**
- **Progression conditions** - Merchant requires wealth, Nurse increased health after Merchant arrival, Arms Dealer bullet gear, Demolitionist explosives, Dryad a boss defeat, Goblin Tinkerer a completed Goblin Army, Mechanic Skeletron, and Wizard Hardmode.
- **Monotonic population** - arrival flags are permanent once earned; spent wealth, ammunition, or explosives cannot remove residents. The one-second evaluator feeds the existing additive/deduplicating `spawnNpcs()` path.
- **Persistence and migration** - arrival flags and event-completion records round-trip through saves. Legacy format-1 saves reconstruct them from saved NPC/invasion achievements, while fresh worlds remain isolated from the global achievement registry.
- **Smoke test: 22/22 checks PASS, zero browser errors** (fresh-world gates, all eight triggers, requirement spending, Goblin completion semantics, duplicate prevention, save/load persistence, legacy migration, new-world isolation, contextual service access, and 120 clean `step(1/60)` frames). `js/main.js` passes the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 17 - complete town NPC arrivals:**
- **Full roster progression** - fresh worlds now begin with only the Guide. Dye ownership, total kills, Ocean/Underground Desert/Spider Cave/Mushroom discovery, population milestones, bosses, invasions, Hardmode, individual mechs, Plantera, and Frost Legion drive the remaining town arrivals.
- **Late residents** - Painter and Party Girl use population milestones, post-mech residents retain their progression gates, and Princess requires all 23 non-seasonal, non-Princess residents.
- **Migration safety** - a completion marker distinguishes fully gated saves from Batch 16 saves. Older NPC achievements are merged once to preserve prior residents, while complete saves and new worlds do not import unrelated global achievement state.
- **Smoke test: 21/21 checks PASS, zero browser errors** (Guide-only fresh world, inventory/boss/invasion/biome/kill/population/Hardmode arrivals, all 25 residents, deduplication, complete and legacy migration paths, new-world isolation, loop evaluation, and 120 clean frames). `js/main.js` passes the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 18 - town housing (latest):**
- **Buildable safe rooms** - added craftable Wood Walls, direct background-wall placement, and hammer removal/recovery. Housing flood-fill requires an enclosed 20-300 tile room covered by player-placed Wood Walls with a Torch, Table, Chair, and clear floor space; natural cave walls are rejected.
- **Assignment UI** - added a responsive Housing tab that reports the room around the player and lists every arrived resident. A valid room accepts one NPC, immediately relocates them, and supports explicit removal.
- **Structural validity** - assignments are rechecked every two seconds and cleared if walls, enclosure, furniture, lighting, or floor space become invalid.
- **Save integrity** - mutable background walls are now RLE-packed with the tile snapshot; housing coordinates and stable room keys persist, and restored NPCs spawn at home. Saves without a wall payload retain deterministic legacy wall regeneration.
- **Smoke test: 20/20 checks PASS, zero browser errors** (item/recipe/API, room rules, wall placement and hammer recovery, assignment/occupancy/relocation, delegated Housing UI, payload budget, wall/home restoration, invalidation, natural-wall rejection, loop revalidation, and 120 clean frames). Five edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 19 - complete town economy (latest):**
- **Full shop coverage** - Dye Trader, Angler, Zoologist, Painter, Golfer, Pirate, Witch Doctor, Party Girl, Stylist, Clothier, Steampunker, Cyborg, Truffle, Santa, and Princess now join the existing service roster with progression-aware stock drawn from obtainable project items. Tavernkeep keeps his isolated Defender shop.
- **Tax Collector** - a housed Tax Collector gathers Gold Ore once per minute based on housed population, capped at 20. Collection revalidates proximity/inventory capacity and savings persist through saves.
- **Happiness pricing** - every resident has a preferred biome plus liked/disliked neighbor. Housing location, loneliness, preferred company, and overcrowding produce a 75%-150% price multiplier shown in the Town panel and used for the actual transaction.
- **Pylon economy** - Forest, Desert, Snow, Jungle, Hallow, Corruption, Crimson, Ocean, and Universal Pylons are distributed among relevant vendors. Pylon stock appears only when the vendor is housed happily in the Pylon's biome; established crafting recipes remain alternate acquisition routes.
- **Smoke test: 21/21 checks PASS, zero browser errors** (21 shop definitions and item references, shared service coverage, dedicated Tavernkeep routing, every shop render path, progression gates, mood-priced purchases, range safety, Tax Collector accrual/cap/payout/save/UI, preferred biome and neighbor discounts, overcrowding surcharge, and matching/wrong-biome Pylon availability). `js/main.js` passes the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 20 - town NPC rescues (latest):**
- **World encounters** - Angler sleeps at the Ocean, Golfer appears lost in the Underground Desert, Stylist is webbed in Spider Caves, Goblin Tinkerer appears underground after the Goblin Army, Mechanic is bound in the post-Skeletron Dungeon, and Wizard appears in deep underground Hardmode.
- **Explicit rescue flow** - candidates are friendly but cannot open shops, show a dedicated right-click rescue prompt, render with bindings and a HELP marker, and convert in place into their normal resident definition when freed.
- **Persistence and compatibility** - unresolved candidate type/position records round-trip through saves and reconstruct after ordinary entities reset. Rescued residents use the existing monotonic arrival flags, achievements, housing, services, and deduplicating spawner; legacy saves with those residents remain unchanged.
- **Biome correctness fix** - generic underground rescues use `World.isUnderground()` plus depth and mini-biome exclusions because the existing `BIOME.CAVERN`/`BIOME.UNDERGROUND` enum values are not returned by `biomeAt()` for generic caves.
- **Smoke test: 27/27 checks PASS, zero browser errors** (all six passive-arrival removals, biome discovery, prerequisite blocking, candidate/shop isolation, prompts, actual right-click rescue, achievements, unresolved save/load restoration, post-rescue persistence, deduplication, rendering, and 120 clean frames). `js/main.js` and `js/render.js` pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 21 - Angler quests (latest):**
- **Daily request** - the rescued Angler asks for one deterministic fish each world day from the existing Forest, Ocean, evil, Jungle, cave, and Hardmode Hallow fishing catalog. Dawn advances the day and rotates the request; Neon Tetra quests remain Hardmode-only.
- **Safe turn-in flow** - the Town panel reports the requested fish, catch location, inventory count, completed-day status, and lifetime total. Turn-ins require the nearby Angler, consume exactly one fish, and are limited to once per day.
- **Progressive rewards** - every completion grants Gold Ore, Enchanted Nightcrawlers, and a crate. Every third adds a Fishing Potion, every fifth upgrades to an Iron Crate, and every tenth grants a Golden Crate. The full reward bundle is simulated against inventory capacity before the fish is consumed.
- **Persistence and achievement** - world day, last completed day, and lifetime quest count round-trip through saves. The first completion unlocks `anglerquest` ("Servant-in-Training").
- **Smoke test: 17/17 checks PASS, zero browser errors** (deterministic/obtainable requests, Town UI, missing-fish rejection, delegated turn-in, reward bundle, achievement, once-per-day lock, dawn rotation, atomic full-inventory failure, milestone rewards, Hardmode Hallow gating, save restoration, Angler service restoration, and 120 clean frames). `js/main.js` and `js/achievements.js` pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 22 - Guide recipe service (latest):**
- **Material lookup** - selecting any inventory item and opening the Guide page lists every recipe that consumes it, including result icon/name, required station, full material counts, and inferred Pre-Hardmode/Hardmode/post-boss gate. Looking up recipes never changes inventory or bypasses crafting progression.
- **Correct progression help** - removed the obsolete text claiming worlds begin in Hardmode. The Guide now describes the evil-boss/Underworld path before Hardmode and the mechanical-to-Celestial path afterward.
- **Interaction and lifecycle** - `H` remains a global shortcut, while aiming at a nearby Guide provides a right-click recipe prompt and opens the same page. A missing Guide respawns at dawn when no boss is active, including after loading a Hardmode world where ordinary entities reset.
- **Smoke test: 18/18 checks PASS, zero browser errors** (fresh Guide, Pre-Hardmode guidance, Wood multi-recipe lookup, station/phase metadata, no inventory mutation, post-Moon Lord recipe discovery, no-recipe handling, cursor/proximity routing, HUD prompt, Hardmode guidance, boss-delayed dawn respawn, loaded-Hardmode respawn, and 120 clean frames). `js/main.js` passes the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 23 - progression and acquisition integrity (latest):**
- **Health progression** - Heart Crystals now stop at 400 and unlock their achievement only at the cap. Life Fruit requires Hardmode, 400 health, and any mechanical boss, then raises health to 500 and unlocks its own achievement; enemy Life Fruit drops use the same mech gate.
- **One-evil isolation** - Corruption pools no longer contain Ichor Stickers, Wraiths and Black Recluses no longer manufacture opposite-evil blocks/ore, and Eye of Cthulhu drops Demonite or Crimtane according to the selected world evil.
- **Material timing** - Giant Bats no longer drop Soul of Flight. Reaper Ectoplasm waits for Plantera, while Reapers and Frankensteins remain Solar Eclipse-only rather than leaking into ordinary nights.
- **Dormant encounter restoration** - Undead Miners now appear in pre-Hardmode caverns, Skeleton Archers and Toxic Sludges in Hardmode caverns, and Werewolves on ordinary Hardmode surface nights. Their existing AI, rendering, and drops are now reachable.
- **Orphaned rewards** - post-Plantera Dungeon Spirits can drop Magnet Sphere, and Mourning Wood's principal pool now includes Witch's Broom.
- **Smoke test: 23/23 checks PASS, zero browser errors** (Heart Crystal/Life Fruit gates and caps, Soul of Flight/Life Fruit/Ectoplasm timing, neutral and evil-specific drops, Corruption Ichor isolation, ordinary-night Eclipse isolation plus Werewolf routing, all four dormant ambient pools and AI/render paths, Magnet Sphere and Witch's Broom acquisition, Crimson Eye ore, and 120 clean Crimson frames). All five edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 24 - recipe and ranged behavior integrity (latest):**
- **One-evil recipe parity** - Obsidian Shield, Obsidian Rose, and Obsidian Skin Potion now consume obtainable Obsidian rather than Ebonstone. Black Dye uses Obsidian and Soul of Night instead of the impossible Ichor-plus-Ebonstone combination, so explicit Crimson worlds can craft every neutral item without cross-evil leakage.
- **Ranged volleys** - `Player.tryShoot` now interprets existing ranged `spread` metadata as projectile count and emits an evenly centered cone. Shotguns, multishot bows, flamethrowers, and other declared spread weapons now match their item behavior instead of firing one projectile.
- **Ammo and damage integrity** - one compatible selected/fallback ammo item is consumed per trigger, every projectile records that ammo identity and damage, and slot-local reforge multipliers apply consistently. Weapons without spread remain single-shot; magic spread remains on its existing path.
- **Smoke test: 19/19 checks PASS, zero browser errors** (four neutral recipe material checks, end-to-end Crimson crafting, opposite-evil absence, Shotgun count/ammo/damage/centered trajectories, single-shot regression, selected Ichor ammo, reforge scaling, Tsunami/Chain Gun counts, no-ammo block, magic spread isolation, and 120 clean projectile frames). Both edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 25 - melee projectile behaviors (latest):**
- **Sword projectiles** - Excalibur, Chlorophyte Saber, Terra Blade, Frostbrand, Beam Sword, Death Sickle, True Excalibur, and True Night's Edge retain their close arc and now emit themed, reforged-damage projectiles. Ordinary swords remain arc-only.
- **Thrown and returning weapons** - Shadowflame Knife is projectile-only and bounces from tiles. Light Disc, Paladin's Hammer, Possessed Hatchet, and Bananarang fly outward, damage each target at most once, survive enemy contact, reverse on range/wall limits, and despawn when they return to the player; Possessed Hatchet homes during its outbound phase.
- **Vampire Knives** - each use throws a centered five-knife spread with reduced per-knife damage and heals from actual projectile contacts instead of applying the generic local sword arc.
- **Shared projectile support** - melee projectile metadata carries type/speed/lifetime/count/spread/damage/return/homing/bounce/persistence/life-steal state. Persistent projectiles track entity/segment references to prevent repeated frame damage, and Razor/Dart rendering honors item color.
- **Smoke test: 16/16 checks PASS, zero browser errors** (14 metadata definitions, sword beam and ordinary-sword regression, reforge scaling, projectile-only knife collision, Vampire count/healing, two-target persistent disc hits with repeat immunity, return/despawn, Hatchet homing, Shadowflame tile bounce, rendering, and 120 clean frames). All four edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 26 - spear and polearm behaviors (latest):**
- **Attached thrusts** - Gungnir, Titanium Trident, Adamantite Glaive, Dark Lance, Rotted Fork, Mushroom Spear, North Pole, and Chlorophyte Partisan now use a player-attached spear projectile instead of the generic broad sword arc. The tip extends to maximum range halfway through the attack and retracts for the remainder.
- **Collision integrity** - thrusts follow player movement and aim, carry accessory/reforge-adjusted melee damage, survive their first contact, and use projectile-local target references so one attack cannot repeatedly damage the same entity.
- **Themed effects** - Mushroom Spear emits a bouncing spore, North Pole emits a Frost Bolt, and Chlorophyte Partisan emits a living spore. Secondary damage scales from the same reforged melee damage.
- **Rendering and lifecycle** - `P.STINGER` honors item color and draws an extended shaft for attached spears. Thrusts disappear after retraction or immediately if their player dies; ordinary swords and previous returning weapons remain unchanged.
- **Smoke test: 16/16 checks PASS, zero browser errors** (eight metadata definitions, extension/retraction, player following, local-arc suppression, tip damage with repeat immunity, reforge scaling/range, all three secondary effects, ordinary-sword regression, cleanup, colored rendering, and 120 clean frames). All four edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 27 - flail behaviors (latest):**
- **Tethered lifecycle** - Flairon, Dao of Pow, Chain Guillotines, Drippler Crippler, Sunfury, and Ball O' Hurt now throw outward, remain extended for a swinging phase, and retract to the moving player instead of using the generic sword arc.
- **Collision integrity** - each flail carries accessory/reforge-adjusted melee damage, remains active after contact, and tracks hit entity/segment references so a single throw cannot deal repeated frame damage to the same target.
- **Flairon bubbles** - each Flairon throw also launches three reduced-damage homing Magic Bolts from the same reforged melee damage base.
- **Rendering** - flail heads use the existing colored Razor visual and draw a matching two-pixel tether from the player to the projectile throughout the attack.
- **Smoke test: 16/16 checks PASS, zero browser errors** (six metadata definitions, outbound/held/retract motion, player following, local-arc suppression, collision/repeat immunity, reforge range/damage, Flairon bubble count/homing, tether rendering, cleanup, spear/returning/sword regressions, and 120 clean frames). All four edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 28 - yoyo behaviors and acquisition (latest):**
- **Held yoyo lifecycle** - The Eye of Cthulhu, Hel-Fire, Yelets, Kraken, Terrarian, Amarok, and Gradient now extend toward the cursor, remain range-clamped on a rendered string, follow cursor/player movement, and retract after their item-specific duration. Only one yoyo can remain active per player.
- **Collision behavior** - yoyos use accessory/reforge-adjusted melee damage and local 0.35-second target immunity, allowing controlled repeated damage while held without frame-by-frame hits.
- **Signature projectiles** - Yelets periodically fires spores and Terrarian fires rainbow blades from the held yoyo using scaled weapon damage.
- **Acquisition integrity** - Hel-Fire, Yelets, Amarok, and Kraken now come from Hardmode Underworld, post-mech Jungle, Snow, and post-Plantera Dungeon enemies. Terrarian now drops from Moon Lord. Their direct recipes were removed; Gradient's recipe remains as the project's Skeleton Merchant substitute.
- **Smoke test: 23/23 checks PASS, zero browser errors** (seven definitions, extension/hold/retraction, cursor tracking, range clamp, one-active limit, reforge damage, repeat-hit cooldown, both signature projectiles, rendering, recipe removal, all five drop/boss sources, prior melee regressions, and 120 clean frames). All six edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 29 - controlled and payload melee (latest):**
- **Flying Knife control** - the Hallowed Mimic dagger now flies toward the cursor, remains limited to a 12-tile radius, follows player movement, damages targets on a local cooldown, renders as a blade, permits one active knife, and returns when the attack is released or terrain blocks its path.
- **Starfury strike** - each Starfury swing retains its local arc and calls a falling Blaze projectile above the cursor using accessory/reforge-adjusted melee damage.
- **Seedler and Scourge payloads** - Seedler launches a bouncing seed that bursts on contact into three homing spores. Scourge is now a projectile-only corrupt javelin that releases three reduced-damage homing eaters.
- **Acquisition integrity** - Seedler is now a guaranteed simplified Plantera reward and Scourge joins the Corrupt Mimic principal pool; both direct recipes were removed. Starfury sky-chest and Flying Knife Hallowed Mimic acquisition remain intact.
- **Smoke test: 25/25 checks PASS, zero browser errors** (four metadata paths, controlled movement/cursor/range/single-active/release return, reforge scaling, Starfury rain, both payload splits/homing, recipe removals, three boss/Mimic sources, rendering, five prior melee regressions, and 120 clean frames). All seven edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 30 - channelled magic (latest):**
- **Last Prism beam** - Last Prism now creates one persistent rainbow beam attached to the player rather than a stream of short bolts. It tracks the cursor, reaches 42 tiles, stops at solid terrain, renders layered animated colors, and can strike multiple entities and segmented-boss parts along its line.
- **Channel resources and safety** - the beam drains accessory-adjusted mana every 0.12 seconds, applies slot-local reforge damage with matching target immunity, and ends on mouse release, player death, panel opening, mana exhaustion, or its safety lifetime. Repeated input cannot create duplicate beams.
- **Laser Machinegun spin-up** - continuous firing progressively reduces its delay from 0.16 to 0.05 seconds while narrowing aim variance; releasing the attack or switching back later resets the ramp.
- **Acquisition integrity** - Heat Ray's direct recipe was removed and Golem now supplies it as a guaranteed simplified reward. Last Prism and Laser Machinegun retain their Moon Lord and Martian Saucer sources.
- **Smoke test: 24/24 checks PASS, zero browser errors** (metadata, beam creation/uniqueness/aim/range/wall collision/multi-target damage/immunity/mana/release/death cleanup, rainbow rendering, machinegun ramp/reset, Heat Ray and existing weapon sources, ordinary spread-magic regression, and 120 clean frames). All six edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 31 - deployed magic (latest):**
- **Nimbus clouds** - Nimbus Rod now places stationary six-second storm clouds at the cursor. Each periodically rains damaging vertical bolts, and placing a third cloud replaces the oldest so two can remain active.
- **Magnet Sphere** - the Dungeon Spirit weapon now launches one slow drifting sphere that periodically targets nearby enemies with fast laser shots. Recasting replaces the existing sphere.
- **Clinger wall** - Clinger Staff now places one seven-tile vertical cursed-flame wall with local target immunity and repeated area damage. Recasting relocates the wall rather than stacking it.
- **Integrity and rendering** - all deployments consume accessory-adjusted mana once, carry slot-local reforge damage into their emitted or area attacks, expire safely, and have distinct procedural cloud/orb/flame visuals. Nimbus Rod's direct recipe was removed because Angry Nimbus already supplies it; Magnet Sphere and Clinger Staff retain Dungeon Spirit and Corrupt Mimic sources.
- **Smoke test: 23/23 checks PASS, zero browser errors** (three metadata paths, cloud placement/rain/replacement, sphere movement/targeting/replacement, wall damage/immunity/replacement, mana/reforge scaling, expiry, rendering, recipe removal, all three drops, Last Prism and ordinary magic regressions, and 120 clean frames). All five edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 32 - terrain-targeted attacks (latest):**
- **Daedalus Stormbow** - the Hallowed Mimic bow now rains three copies of the currently selected compatible arrow around the cursor while consuming exactly one ammunition item per trigger. Each projectile preserves ammo identity, ammo damage, and ranged reforge/accessory scaling.
- **Magic skyfall** - Blizzard Staff calls four Frost Bolts down around the cursor, while Lunar Flare calls two Fireballs that pass through terrain before striking. Both consume mana once per cast and retain magic reforge/accessory damage.
- **Blood Thorn eruption** - Blood Thorn now sends three upward projectiles from below the cursor, passing through terrain to reach enemies rather than firing a generic forward homing spread.
- **Acquisition integrity** - Lunar Flare's direct recipe was removed and Moon Lord now provides it alongside Last Prism. Blizzard Staff, Stormbow, and Blood Thorn retain Ice Queen, Hallowed Mimic, and Hemogoblin Shark sources.
- **Smoke test: 22/22 checks PASS, zero browser errors** (four metadata paths, selected Holy Arrow identity/one-ammo consumption/damage, all three terrain geometries, terrain pass-through, mana/reforge scaling, rendering, recipe removal, all four sources, ordinary Shotgun/magic regressions, and 120 clean frames). All five edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 33 - ricochet and explosive projectiles (latest):**
- **Ricochet behavior** - Pulse Bow energy bolts now bounce from terrain three times and Shadowbeam Staff bolts bounce four times. Ranged and magic firing now preserve declared bounce metadata instead of dropping it at projectile creation.
- **Homing ammunition** - Chlorophyte Bullets and the Chlorophyte Shotbow now transfer homing behavior into every fired projectile while preserving selected ammo identity, damage, and consumption.
- **Explosive ammunition** - Explosive Bullets, Hellfire Arrows, Grenades, and Rocket I-IV now carry explicit blast radii. They detonate on enemy impact, terrain impact, or lifetime expiry, damage nearby enemies for reduced splash damage, avoid double-hitting the direct target, and scale Rocket I-IV from 48 to 72 pixels.
- **Rendering and integrity** - selected arrow/bullet colors now reach projectile rendering. Existing ammo recipes remain intact, while Musket Balls and ordinary spread magic remain non-explosive/non-ricocheting.
- **Smoke test: 19/19 checks PASS, zero browser errors** (metadata, Pulse/Shadowbeam wall ricochets, Chlorophyte homing, direct-plus-splash damage and radius cutoff, one explosion, rocket wall and bullet expiry detonations, colored rendering, recipe integrity, ordinary ammo/magic regressions, and 120 clean frames). All four edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 34 - launcher and proximity mines (latest):**
- **Launcher physics** - Grenade Launcher shells now gain downward velocity throughout flight, producing a real lob while retaining selected grenade damage and splash behavior. Snowman Cannon rockets now acquire nearby enemies, and Onyx Blaster's dark shard carries a weapon-level explosion radius.
- **Proximity mines** - Proximity Mine Launcher shots inherit the selected compatible rocket's damage and blast radius, arc under gravity, arm without detonating when they touch terrain, render as blinking ground mines, and explode when an enemy enters a 60-pixel trigger radius or the eight-second fuse expires.
- **Acquisition integrity** - Grenade Launcher is now a guaranteed simplified Plantera reward and Proximity Mine Launcher is sold by the Cyborg after Plantera. Their direct recipes were removed; Onyx Blaster crafting and Ice Queen's Snowman Cannon reward remain intact.
- **Smoke test: 19/19 checks PASS, zero browser errors** (four metadata paths, grenade gravity, selected Rocket IV mine damage/radius, terrain arming, wait/trigger/expiry detonation, mine rendering, Onyx splash, Snowman homing, recipe removal, Plantera/Cyborg sources, ordinary launcher/explosive-ammo regressions, and 120 clean frames). All six edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 35 - guided and piercing magic (latest):**
- **Guided projectiles** - Flamelash and Rainbow Rod now create one cursor-guided projectile rather than generic forward or homing spreads. They use item-specific 24/32-tile control ranges, speeds, and safety durations while consuming mana once and applying slot-local reforge damage.
- **Control safety** - guided magic cleans up on release, panel opening, player death, terrain collision, or enemy impact, and repeated input cannot create a duplicate projectile. The existing Flying Knife return lifecycle remains independent.
- **Piercing magic** - Crystal Vile Shard projectiles persist through enemy contact and track target references, allowing one shard to hit multiple entities without repeated frame damage to the same target.
- **Acquisition integrity** - Rainbow Rod's recipe, Flamelash Shadow Chest source, and Crystal Vile Shard Hallowed Mimic source remain intact.
- **Smoke test: 25/25 checks PASS, zero browser errors** (three metadata paths, creation/mana/reforge, cursor tracking/range/single-active limit, all cleanup paths, impact, piercing multi-target immunity, rendering, three acquisition checks, ordinary magic and Flying Knife regressions, and 120 clean frames). All three edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 36 - projectile status effects (latest):**
- **Damage-over-time effects** - Cursed Bullets/Darts/Flames apply four-second cursed flame, Venom Bullets/Darts/Staff apply five-second venom, and Frostburn Arrows apply four-second frostburn. Effects tick once per second for fixed damage independent of defense.
- **Ichor defense reduction** - Ichor Bullets/Darts and Golden Shower reduce effective enemy and boss defense by 12 for five seconds, affecting direct entity, boss, pillar, segmented-head, and segment damage paths.
- **Shared lifecycle** - projectile status metadata now transfers from selected ammunition or magic weapons, first applications show floating feedback, repeat hits refresh duration without duplicating timers, and effects expire cleanly. Ordinary bullets and unrelated magic remain status-free.
- **Smoke test: 25/25 checks PASS, zero browser errors** (ten metadata sources, selected bullet/arrow/dart and magic transfer, feedback, delayed exact damage ticks, refresh/expiry, ichor penalty/direct damage, boss venom, recipe integrity, ordinary projectile regressions, and 120 clean frames). All five edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 37 - Life Drain channel (latest):**
- **Crimson channel** - Life Drain now creates one 18-tile crimson beam attached to the player instead of firing generic Spore projectiles. It follows the cursor, stops at terrain, and uses its own layered blood renderer without changing Last Prism's rainbow presentation.
- **Contact healing** - beam contacts heal 8% of reforged weapon damage per target, respect a 0.2-second local target cooldown and maximum health, and support draining several enemies along the line.
- **Resources and safety** - Life Drain consumes accessory-adjusted mana on cast and every 0.18 seconds, and ends on release, mana exhaustion, panel opening, player death, or safety expiry. Duplicate channels are blocked.
- **Acquisition integrity** - Life Drain remains recipe-free and available from the Crimson Mimic principal pool.
- **Smoke test: 22/22 checks PASS, zero browser errors** (metadata, creation/style/reforge/mana, uniqueness, range, damage/healing/immunity/multi-target/cap, periodic drain, terrain, release/exhaustion/panel/death cleanup, rendering, acquisition, Last Prism and ordinary magic regressions, and 120 clean frames). All three edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 38 - whip and minion integrity (latest):**
- **Timed whip tags** - all nine whips now apply four-second minion tag bonuses scaled from Leather Whip's +4 through Kaleidoscope's +20. Whip reach uses each item's range metadata, tagged enemies receive target priority, and expired tags clear both priority and bonus damage.
- **Minion combat repair** - projectiles owned by minions now route through enemy collision rather than player collision. Direct dragon/spider/blade attacks and generic minion projectiles add the active whip tag bonus, while attack cooldowns prevent melee summons from dealing frame-by-frame damage.
- **Friendly safety** - nearest-enemy searches, ordinary projectile collision, beams, deployed zones, and explosions consistently exclude friendly minions so summons cannot absorb player or summon attacks.
- **Acquisition integrity** - Kaleidoscope's old direct recipe was removed because Empress already guarantees it; Dark Harvest remains a Pumpking reward.
- **Smoke test: 20/20 checks PASS, zero browser errors** (nine metadata definitions, range/tag strength, reforge damage, duration/expiry, target priority, projectile/direct tag damage, ownership/player safety, direct cooldown, friendly projectile exclusion, recipe removal, both boss sources, and 120 clean frames). All five edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 39 - special whip effects (latest):**
- **Firecracker** - whip hits arm a one-use explosive tag. The next direct or projectile minion contact consumes it and deals scaled area damage around the target.
- **Dark Harvest** - tagged targets retain a repeatable dark-energy splash that triggers on every minion contact until the four-second tag expires.
- **Cool Whip** - successful strikes release one reduced-damage homing Frost Bolt from the whip tip.
- **Snapthorn and Durendal** - successful hits grant four seconds of whip-only attack-speed acceleration, with Durendal providing the stronger bonus. The cooldown system now honors each whip's own speed definition.
- **Lifecycle integrity** - tag expiry clears burst/splash state, applying another whip replaces prior special state, and ordinary whips keep tag-only behavior. Boss-exclusive Kaleidoscope and Dark Harvest remain recipe-free.
- **Smoke test: 20/20 checks PASS, zero browser errors** (metadata, one-shot/repeatable splash, direct/projectile minion triggers, Cool Whip bolt, speed strength/use/expiry, special cleanup, ordinary-whip and acquisition regressions, and 120 clean frames). All four edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 40 - summon staff and contact minions (latest):**
- **Reusable summon weapons** - `Player.tryMinion` no longer consumes summon staves. Recasting still replaces the oldest minion at the base cap, minion-cap accessories extend that cap, and the staff remains in its slot for repeated commands.
- **Summon damage scaling** - newly spawned minions snapshot the player's current accessory damage multiplier, then continue to receive timed whip-tag bonuses through the repaired shared minion damage path.
- **Flying contact summons** - Abigail, Raven, and Sanguine Bat now pursue and touch enemies with distinct movement speed/cooldowns rather than hovering and firing unrelated Phantom Bolt or Stinger projectiles.
- **Ground contact summons** - Desert Tiger and Pirate now pursue along terrain, jump toward separated targets, and deal cooldown-limited contact attacks. Imp and other genuine ranged summons retain their projectile behavior.
- **Acquisition integrity** - Abigail and Desert Tiger retain project substitute recipes, while Raven, Sanguine, and Pirate Staff retain Pumpking, Dreadnautilus, and Flying Dutchman sources.
- **Smoke test: 18/18 checks PASS, zero browser errors** (staff reuse, base/accessory caps, accessory damage scaling, five contact paths and cooldowns, ranged Imp regression, whip-tag damage, two substitute recipes, three drop sources, and 120 clean frames). Both edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 41 - ranged summon signatures (latest):**
- **Status projectiles** - Imp fireballs and the cursed Optic shot apply cursed flame, while Hornet and Pygmy stingers apply venom through the shared projectile-status lifecycle.
- **Distinct firing patterns** - Optic alternates a fast laser with cursed flame, Tempest fires a twin homing volley, Xeno fires the fastest summon laser, Stardust Cell fires one persistent homing bolt, and Deadly Sphere fires two spread stingers.
- **Combat integration** - every projectile retains minion ownership, accessory-scaled base damage, active whip-tag bonus damage, status application, friendly safety, and the completed projectile collision behavior.
- **Acquisition integrity** - Pygmy Staff and Tempest Staff direct recipes were removed; Plantera and Duke Fishron now guarantee them in the simplified boss rewards. Optic, Hornet, and Stardust Cell crafting and Xeno/Deadly Sphere drops remain intact.
- **Smoke test: 18/18 checks PASS, zero browser errors** (all seven shot signatures, Optic alternation, speed/count/homing/piercing/status fields, whip-tag plus venom contact, contact-minion regression, recipe migration, Plantera/Duke/Xeno sources, and 120 clean frames). All three edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 42 - boss reward acquisition integrity (latest):**
- **Wall of Flesh rewards** - each defeat retains the Pwnhammer and now rolls one Clockwork Assault Rifle/Firecracker weapon plus one Warrior/Magic/Ranger/Summoner Emblem, replacing six direct recipe shortcuts.
- **Hardmode boss pools** - Queen Slime now supplies Blade Staff. Golem retains Picksaw and rolls Heat Ray, Possessed Hatchet, Staff of Earth, or Sun Stone. Duke Fishron rolls one principal weapon from Razorblade Typhoon, Tempest Staff, Flairon, Tsunami, or Bubble Gun plus Fishron Wings or Shrimpy Truffle.
- **Late summon/endgame rewards** - Empress now supplies Terraprisma alongside Kaleidoscope, while Moon Lord supplies Celebration alongside Last Prism, Lunar Flare, Stardust Dragon Staff, and Terrarian.
- **Acquisition integrity** - seventeen direct recipes for these boss-exclusive items were removed. Every migrated item remains obtainable from its implemented encounter, and Wall of Flesh still triggers Hardmode through the same reward path.
- **Smoke test: 23/23 checks PASS, zero browser errors** (recipe removal, item definitions, both Wall weapon/emblem extremes and pool cardinality, Hardmode trigger, all four Golem rolls, all five Duke principal rolls/two mobility rolls, Queen Slime/Empress/Moon rewards, and 120 clean frames). Both edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 43 - Mimic and pet acquisition integrity (latest):**
- **Ordinary Mimic rewards** - ordinary Mimics now roll one Philosopher's Stone, Star Cloak, Cross Necklace, or Dual Hook instead of placeholder Hallowed Bars. All four direct recipe shortcuts were removed and source descriptions now identify Mimics.
- **Corruption pets** - the first broken Shadow Orb in a Corruption world grants the Shadow Orb light pet alongside the existing rotating classic weapon, Demonite, warning, and boss-trigger progression. The Eater of Worlds now has a repeatable rare Baby Eater pet drop.
- **One-evil integrity** - Crimson Hearts never grant the Corruption light pet. Purple Dye now uses Amethyst rather than consuming Shadow Orb, preserving neutral dye crafting in explicit Crimson worlds.
- **Smoke test: 22/22 checks PASS, zero browser errors** (all four Mimic rolls, Hallowed Bar removal, recipe removal, source descriptions, neutral Purple Dye, pet definitions, Corruption and Crimson Orb/Heart behavior, retained classic loot/ore, Baby Eater boss drop, both pet activation paths, render, and 120 clean frames). All five edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 44 - class-specific accessory damage (latest):**
- **Damage channels** - accessory aggregation now exposes universal, melee, ranged, magic, and summon multipliers. A shared inventory helper combines the requested class channel with universal damage without changing slot-local reforge scaling.
- **Metadata repair** - Warrior/Magic/Ranger/Summoner Emblems now affect only their named classes. Fire Gauntlet and Mechanical Glove are melee-only; Rifle Scope, Sniper Scope, and Magic Quiver are ranged-only; Hercules Beetle, Papyrus Scarab, Necromantic Scroll, and Apprentice's Scarf are summon-only.
- **Combat integration** - melee attacks and projectiles, ranged ammunition, magic attacks, whips, spawned minions, and Old One's Army sentries consume their correct class multiplier. Whips count as summon damage while universal accessories continue affecting every class.
- **Smoke test: 20/20 checks PASS, zero browser errors** (metadata channels, four emblem isolation paths, universal and same-class multiplicative stacking, minion capacity retention, actual melee/ranged/magic/minion/sentry/whip consumers, wrong-class combat isolation, render, and 120 clean frames). All four edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 45 - explicit magic volley and homing metadata (latest):**
- **Independent volley metadata** - fourteen multishot magic weapons now use `projCount` rather than overloading ranged `spread`. Laser Machinegun remains an explicit single shot, and Old One's Army spell rewards follow the same contract.
- **Intentional tracking** - projectile creation now reads only `projHoming`; firing multiple bolts no longer grants tracking. Razorblade Typhoon, Venom Staff, Spirit Flame, Weather Pain, and the single-shot Spectre Staff explicitly retain or gain their intended homing behavior.
- **Behavior integrity** - straight volleys preserve centered trajectories, statuses still transfer, each cast consumes mana once, class/reforge damage remains intact, and ranged weapon spread is isolated from the magic path.
- **Smoke test: 20/20 checks PASS, zero browser errors** (all fourteen volley definitions, no residual magic `spread`, five homing declarations, straight-volley isolation, centered trajectories, one-cost casting, status transfer, Army magic, machinegun single shots, class damage, terrain magic, ranged spread, render, and 120 clean frames). All three edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 46 - expanded deployed magic signatures (latest):**
- **Crimson Rod cloud** - Crimson Rod now places one six-second crimson cloud at the cursor and periodically rains reduced-damage blood-colored Cursed Flame projectiles. Recasting relocates its cloud instead of firing a generic forward bolt.
- **Nebula Arcanum orb** - Nebula Arcanum now creates one drifting six-second sphere that periodically acquires nearby enemies and emits reduced-damage homing Phantom Bolts. Magic accessories and slot-local reforges scale the orb before its payload multiplier.
- **Deployment isolation** - deployment replacement now uses the source item identity as well as mode. Crimson Rod can coexist with Nimbus Rod, and Nebula Arcanum can coexist with Magnet Sphere, while each weapon still replaces its own oldest deployment at its cap.
- **Rendering and acquisition** - clouds, spheres, Cursed Flames, and Phantom Bolts honor source colors. Crimson Rod remains rotating Crimson Heart loot and Nebula Arcanum retains its fragment recipe.
- **Smoke test: 20/20 checks PASS, zero browser errors** (metadata, both acquisition routes, placement/mana/identity, scaled rain, class/reforge orb damage, homing emission, same-weapon replacement, cross-weapon coexistence, legacy deployment damage, rendering, and 120 clean frames). All four edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 47 - Windy Day (latest):**
- **Natural weather event** - strong daytime wind now enters Windy Day above 20 mph and leaves below 16 mph, remains surface-only, coexists with rain, and persists through saves. It intentionally does not use invasion waves, a summon item, or completion rewards.
- **Ambient encounters** - Angry Dandelions anchor to exposed Forest/Hallow grass and launch seeds downwind; Windy Balloon Slimes drift from upwind and become ordinary slimes when their balloons pop; Ladybugs ride the wind as passive critters.
- **Items and interaction** - added Daybloom, seven enemy-dropped kite styles, two stackable Paper Airplanes that curve with the wind and can be recovered, and Windy Day Pin Wheel stock for the Merchant. Kites remain attached to the player, respond to movement and wind, and unlock the Windy Day achievement when flown.
- **Presentation** - cloud speed and airborne leaves react to wind while dedicated enemy, seed, airplane, and kite renderers preserve the existing procedural-art style. The HUD identifies active Windy Day conditions without replacing ordinary weather text.
- **Smoke test: 32/32 checks PASS, zero browser errors** (timing/hysteresis, rain coexistence, surface restriction, persistence, natural pool/upwind placement, exposed-grass Dandelions, downwind attacks, balloon transition, all item/drop routes, kite controls, Paper Airplane recovery, Merchant stock, rendering, and 120 clean frames). All seven edited JS files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 48 - Party and Lantern Night (latest):**
- **Natural and manual Parties** - eligible towns can begin a natural Party at dawn after the vanilla-style cooldown, while a placeable Party Center toggles manual celebrations. Natural Parties cannot be stopped early and grant one Slice of Cake when the player first visits the Party Girl.
- **Party content** - added Party Center, Party Hat, Party Present, Pigronata, grouped Streamers and Silly Balloons, and Slice of Cake. Party Girl and Clothier stock changes during Parties; gift items open into party loot, Sugar Rush grants reusable two-minute movement/mining speed, NPCs wear hats, and the sky gains balloons/confetti.
- **Victory celebrations** - first qualifying boss and invasion victories queue a Lantern Night, with the Eater/Brain sharing one qualification and Old One's Army covered. Hostile events and bosses defer queued nights or interrupt active ones; completed nights run dusk-to-dawn and expose the vanilla `+0.3` luck state.
- **Lantern content** - Party Girl sells Release Lanterns during Lantern Nights; used lanterns consume one item, rise for sixteen seconds, drift with wind, and glow procedurally. Peaceful-state HUD, dialogue, achievements, cooldowns, first-clear records, active state, and pending state persist through saves.
- **Smoke test: 32/32 checks PASS, zero browser errors** (item/tile integrity, peaceful-state isolation, natural Party gates/cooldown, Party shop/Cake/Sugar Rush, Party Center behavior, dusk ending, first-clear boss/invasion/Army scheduling, duplicate prevention, hostile deferral/interruption, luck, Lantern shop/use/motion, save fields, rendering, HUD, and 120 clean frames). All seven edited JavaScript files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 49 - Starfall and Fallen Stars (latest):**
- **Version-correct world state** - Terraria 1.4.4 uses Starfall; the damaging Meteor Shower belongs to 1.4.5. Each dusk now rolls the canonical 10% Starfall chance and a 3x-5x star multiplier, while failed rolls can still receive the vanilla-style 1x-1.5x minor nightly boost.
- **Falling-star gameplay** - world-owned stars fall near the active player, glow, collide with terrain/platforms, become persistent Fallen Star pickups, and deal 1,000 base damage to hostile entities without damaging players. Every exposed Fallen Star pickup, including manually dropped ones, disappears at dawn while inventory/chest stars remain safe.
- **Nightcrawler acquisition** - clear Starfall skies periodically produce collectible Enchanted Nightcrawlers at exposed grass. Rain blocks them, Lantern Night can overlap independently, and its `+0.3` luck shortens the natural spawn interval.
- **Recipes and presentation** - added the Fallen Star material, canonical one-Worm/one-Star Nightcrawler recipe, and ten-Arrow/one-Star Jester's Arrow recipe. Starfall adds background streaks, a distinct repaired `P.STAR` renderer, projectile/pickup light, HUD text, save restoration, and How To Play guidance.
- **Smoke test: 32/32 checks PASS, zero browser errors** (item/recipe integrity, exact active/minor/normal rolls, Lantern overlap, baseline stars, player safety, enemy damage, terrain landing, dawn pickup/inventory rules, end-to-end bait crafting, clear/rainy Nightcrawler behavior, luck, dusk/dawn transitions, hostile coexistence, persistence, HUD, rendering, and 120 clean frames). All four edited JavaScript files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 50 - Graveyards and Ecto Mist (latest):**
- **Death and grave furniture** - every player death now places one recoverable Tombstone on the nearest supported clear tile. Tombstones and Sunflowers are non-solid furniture, require support when placed manually, render distinctly, appear on the minimap, and persist automatically through the existing tile save payload.
- **Derived local biome** - the exact 170x125-tile scene area counts Tombstones minus Sunflowers. Five effective graves activate the Graveyard HUD/palette, seven enable encounters and Ecto Mist, and visual intensity caps at nine. The overlay intentionally preserves the underlying biome returned by `biomeAt`, keeping Pylons, housing happiness, weather, fishing, rescue gates, and boss restrictions intact.
- **Ghost encounter** - added the classic 50-health, 15-damage, 4-defense Ghost with wall-phasing pursuit, working contact damage, procedural rendering, and coin drops. Functional surface/cavern Graveyards spawn Ghosts and night enemies during daytime, weaken Hardmode town-safe suppression, and use the 1.4.4 50% Tombstone-break Ghost chance with a two-Ghost cap.
- **Ecto Mist content** - environmental station flags combine Ecto Mist with nearby Workbenches or Anvils. Added Gravedigger's Shovel, both evil variants of Shadow Candle, Tattered Wood Sign, Dryad Sunflower stock, station labels, mist/wisps, darker lighting, and the Quiet Neighborhood achievement.
- **Smoke test: 32/32 checks PASS, zero browser errors** (tile/item/enemy integrity, non-solid furniture, five/seven thresholds, underlying biome preservation, Sunflower cancellation, Ecto stations/labels/recipes and end-to-end craft, death placement/save persistence, daytime pool, Ghost AI/contact/drop/break chance/cap, town-safe weakening, shop, achievement, sprites/minimap, rendering, and 120 clean frames). All nine edited JavaScript files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 51 - Shimmer transmutations (latest):**
- **Dropped-item interaction** - pickups immersed in the generated Aether pool now transmute in place, preserve their count, lose invalid reforge metadata, emit Shimmer rings/text/audio, and persist their transformed identity and immersion lock through saves.
- **Progression-safe catalog** - added the complete one-way 1.4.4 ore chain from Luminite through Copper to Stone, the Diamond-through-Amethyst gem chain, Marble/Granite pair, four-class Emblem cycle, three Pirate-accessory cycle, and selected Ice/Sandstone/wood downgrades.
- **Exploit exclusions** - no ore transforms upward; Demonite/Crimtane and their boss materials remain isolated; ordinary crates cannot become Hardmode crates; healing/mana potions do not decraft; blanket recipe reversal remains disabled until boss-material, Bone, Temple, and multi-recipe rules are audited.
- **Pickup integrity** - a transformed pickup changes once per immersion rather than cycling every frame. Generic partial inventory collection now subtracts only the accepted amount instead of deleting the entire remaining world stack.
- **Smoke test: 20/20 checks PASS, zero browser errors** (Aether detection, ore/gem chains, alternate ores, Emblem/Pirate cycles, evil/crate/potion exclusions, transformation/count/reforge/lock/FX, Marble pair, outside-pool safety, persistence, rendering, and 120 clean frames). Both edited JavaScript files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 52 - Shimmer permanent upgrades (latest):**
- Added Vital Crystal, Aegis Fruit, Ambrosia, and Advanced Combat Techniques: Volume Two as one-way Shimmer outputs from Heart Crystal, Life Fruit, Golden Apple, and Spell Tome.
- Each upgrade can be consumed once per player; duplicates remain intact after a blocked second use. Vital Crystal adds permanent regeneration, Aegis Fruit adds 4 defense, Ambrosia adds 5% mining speed, and the combat volume records permanent resident training.
- All four player flags round-trip in the existing save payload and old saves default safely without a format change.
- **Smoke test: 9/9 checks PASS, zero browser errors** (definitions, routes, consumption, duplicate protection, defense, flags, save snapshot, and 120 clean frames). All three edited JavaScript files pass the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 53 - Shimmer bar decrafting (latest):**
- Added canonical ore recovery for all nineteen crafted bar types, with full-stack batching and Hellstone Bar's additional Obsidian output.
- Hallowed Bars remain non-decraftable because they are boss drops. Demonite and Crimtane remain isolated, and equipment, potions, Bone, Temple, and boss-material recipes are still excluded from generic reversal.
- **Smoke test: 8/8 checks PASS, zero browser errors** (route count, Hallowed exclusion, stack ratios, Hellstone/Obsidian output, evil isolation, potion protection, and 120 clean frames). `js/main.js` passes the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 54 - Shimmer equipment decrafting (latest):**
- Added generic recipe-reversal for crafted equipment — weapons, armor, tools, accessories, whips, summon staves, hooks, mounts, pets, and light pets — using a live reverse-map built from `RECIPES` (`SHIMMER_GEAR_DECRAFT`, built once lazily via `buildShimmerGearDecraft()`). New recipes automatically become decraftable.
- Protections: recipes that consume boss materials (souls, Hallowed/Demonite/Crimtane/Luminite bars, Shadow Scale, Tissue Sample), Bone, Temple brick, or consumable ingredients (potions, food) are excluded. Results with more than one eligible recipe are excluded entirely to avoid ambiguous material sets (verified with a synthetic duplicate-recipe injection). The shared `applyDecraft()` helper clears reforge metadata and preserves full-stack batching.
- **Smoke test: 25/25 checks PASS, zero browser errors** (Copper Pickaxe→10 Copper Bar + 4 Wood, eligible gear entries, all exclusion classes, live-map exactness vs the recipe table, synthetic multi-recipe exclusion, end-to-end decraft with reforge clearing, bar-decraft and ore-transmutation regressions, boss-reward non-interference, and 120 clean frames). `js/main.js` passes the ES5 syntax checker.

**Terraria 1.4.4 parity Batch 55 - Shimmer furniture/block decrafting (latest):**
- **Generic furniture reversal** — added `SHIMMER_FURNITURE_DECRAFT` (built once lazily via `buildShimmerFurnitureDecraft()`), a live reverse-map from `RECIPES` for crafted blocks and walls (`SHIMMER_FURNITURE_TYPES = ['block','wall']`). Eight routes auto-derive from the recipe table: Wood Platform, Torch, Glass, Hellstone Brick, Chest, Wooden Chair, Wooden Table, and Wood Wall — each decrafts into its exact recipe materials.
- **Batch-aware counts** — recipes producing more than one item per craft (Platform/Torch/Glass/Wood Wall x4, Hellstone Brick x2) carry the recipe `count` in the map; `applyFurnitureDecraft()` floors to whole craft batches, spawns a leftover pickup for the non-divisible remainder, and converts only complete batches (e.g. 6 Torches → 2 Wood + 2 Torch leftover; 2 Torches alone stay unconverted). Single-craft furniture (Chest/Chair/Table) converts 1:1.
- **Protections** — the same exclusion classes as equipment decrafting apply: boss-material/evil-bar (Shadow Candle), Bone/Temple, and consumable-ingredient recipes are skipped; multi-recipe results are excluded to avoid ambiguous sets; raw blocks with no recipe (Wood/Stone/Marble/Ice) are untouched and keep their existing transmutations. Reforge metadata is cleared and full-stack batching preserved via the shared lifecycle.
- **Smoke test: 39/39 checks PASS, zero browser errors** (exact 8-route id set, per-route materials/counts, Shadow Candle evil-bar exclusion, raw-block transform isolation, Torch 4/8/6/2-count batch math with leftover pickups, Platform/Glass/Hellbrick/Chest/Chair/Table/Wood Wall conversions, real-shimmer-tile integration, non-shimmer safety, Copper bar + Copper Pickaxe + Golden Horseshoe regressions, Wood/Marble transform regressions, and 120 clean frames). `js/main.js` passes the ES5 syntax checker; `git diff --check` passes; throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 56 - Shimmer misc/batch decrafting (latest):**
- **Generic misc/batch reversal** — added `SHIMMER_MISC_DECRAFT` (built once lazily via `buildShimmerMiscDecraft()`), a live reverse-map from `RECIPES` for crafted ammunition, bait, dyes, fishing rods, materials, boss summons, and event items (`SHIMMER_MISC_TYPES = ['ammo','bait','dye','fishingrod','material','summon','eventitem']`). All 46 routes auto-derive from the recipe table (Arrow, Musket Ball, Wooden Dart, Rocket I/II, Grenade, Explosive Bullet, Jester's Arrow, Unholy Arrow, Silk, Tattered Wood Sign, Worm, Nightcrawler, 10 dyes, Wooden/Iron Fishing Rods, Illegal Gun Parts, Slime Crown, Suspicious Eye, Worm Food, Bloody Spine, Abeemination, Goblin Battle Standard, Pirate Map, Solar Tablet, and more) — each decrafts into its exact recipe materials.
- **Batch-aware counts** — every misc route carries the recipe `count` (e.g. Arrow x5/craft, Musket Ball x20, Wooden Dart x50, Silk x2); `applyBatchDecraft()` (shared with the furniture path) floors to whole craft batches, spawns a leftover pickup for the non-divisible remainder with its `shimmered` lock so it cannot re-cycle, converts the primary material into the pickup, and emits extra pickups for multi-material recipes (e.g. Slime Crown → 8 Gold Bar + 10 Gel; Rocket II → 10 Rocket I + 2 Glowstone).
- **Protections** — the same exclusion classes apply: recipes consuming boss materials (souls, Hallowed/Demonite/Crimtane/Luminite bars, Shadow Scale, Tissue Sample), Bone, Temple brick, or consumable ingredients (potions/food/Golden Apple) are skipped; multi-recipe results are excluded (Deer Thing); gear routes (Gravedigger's Shovel is a tool) stay on the equipment path; Pylons and Purification Powder are out of scope. Reforge metadata is cleared and full-stack batching preserved via the shared lifecycle.
- **Smoke test: 77/77 checks PASS, zero browser errors** (route count/exact 46-id set incl. the extra Tattered Wood Sign material route, per-route materials/counts, all exclusion classes, real-shimmer-tile batch math with leftover pickups, one-batch-only conversion (7 Arrows → 1 wood + 2 leftover), end-to-end Slime Crown/Solar Tablet/Silk decrafts, shimmered-lock no-recycle, gear/furniture/bar/transform regressions, and 120 clean frames). `js/main.js` passes the ES5 syntax checker; throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 57 - acquisition integrity audit (latest):**
- **Runtime acquisition audit** — a headless probe verified every item definition is reachable from a real acquisition path. Counted the canonical 679 definitions (658 `defItem` calls in `items.js` + 21 in `oldonesarmy.js`), confirmed the `I` enum and `ITEMS` map keys are identical and every value is a defined item (all named), and confirmed `buildGame()` never mutates the `ITEMS` table.
- **Zero dead definitions** — the static orphan scan now finds **0 orphaned item defs**: 679 defs, and each id appears in at least one acquisition file — recipes (`crafting.js`), drop tables (`entities.js`/`bosses.js`), town/Defender shop stock (`main.js`/`oldonesarmy.js`), chest/world-gen loot (`world.js`), starter items, fishing, events, shimmer decraft/transmutation (`main.js`), or the `TILE_DROP` mining table (`const.js`, which sources the two string-keyed items Tombstone and Hellforge).
- **DUNGEONKEY removed** — the only genuinely dead definition (the legacy Dungeon Golden Key item, superseded by the reusable Shadow Key) was deleted from the `I` enum and `ITEMS`; all 28 `killBoss` reward paths and 221 enemy `dropTable` branches execute cleanly with no undefined-item pickups, and no acquisition source references the removed id.
- **Smoke test: 21/21 checks PASS, zero console errors** (679-def count before build, I↔ITEMS consistency, DUNGEONKEY absence, Shadow Key presence, buildGame no-mutation, TILE_DROP item references, every recipe/town-shop/army-shop/Angler/starter/fish/chest-loot/shimmer id resolves, all enemy + boss + hardmode drop paths run with 0 errors, crimson-chest loot, and 120 clean frames). Static orphan scan: 0 dead defs after including `TILE_DROP` as a mining source. Throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 58 - Shimmer catalog audit completion (latest):**
- **Food decrafting** — added `SHIMMER_FOOD_DECRAFT` (built once lazily via `buildShimmerFoodDecraft()`), an allow-listed (`SHIMMER_FOOD_IDS`) consumable batch path producing exact recipe materials: Pumpkin Pie → 5 Mushroom + 2 Glowstone, Baked Potato → 3 Mushroom + 1 Glowstone, Apple Pie → 4 Mushroom + 2 Glowstone, Burger → 5 Mushroom + 2 Cobweb + 2 Glowstone. Cooked Fish stays excluded (four conflicting recipes = ambiguous) and the 22 potions (including Golden Apple) are sealed by the food allow-list, matching vanilla's no-potion decraft rule.
- **Bar-decraft gap filled** — Meteorite Bar → 4 Meteorite and Shroomite Bar → 1 Mushroom + 1 Chlorophyte Bar were added to `SHIMMER_DECRAFT`; Hallowed Bar remains non-decraftable (boss drop). `buildShimmerBatchDecraft(types, exclude)` gained an optional per-recipe exclusion filter reused by the food builder.
- **Runtime coverage classifier** — an audit function now classifies every one of the **388 unique recipe results**: **232 decraftable** (hardcoded bar routes + gear/furniture/misc/food maps), **2 transform-covered** (Golden Apple → Ambrosia; Wood → Dirt), and **154 intentionally excluded** — 114 consume protected boss materials (souls, Hallowed/Demonite/Crimtane/Luminite bars, Scale/Tissue, Bone, Temple brick), 22 are potions, 9 are Pylons (documented out-of-scope), 6 consume consumable ingredients (Golden Apple), 1 is special (Stone furnace smelt), and 2 are ambiguous multi-recipe results (Cooked Fish, Deer Thing). **0 uncovered recipe results.**
- **Transforms audit** — asserted the ore chain descends strictly (Luminite→…→Copper→Stone), the gem chain descends (Diamond→…→Amethyst→Stone), the only cycles are the intended Emblem, Pirate-accessory, and Marble/Granite loops, and no transform references Demonite/Crimtane or evil stone (no cross-evil leakage).
- **Smoke test: 18/18 checks PASS, zero console errors** (coverage report, zero bugs, excluded + multi breakdowns, exact 4-route food set with correct mats, food allow-list seal, bar routes, ore/gem chain integrity, cross-evil isolation, intended cycles, real-Shimmer e2e for pie/potato/meteorite/shroomite bars, excluded-items stay unchanged, shimmered leftover lock, and 120 clean frames). Throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 59 - vanilla enemy roster and treasure hunting (latest):**
- **7 new vanilla enemies** (`const.js` `E` 276–282; `entities.js` `ENT_DEF` + AI + drops + `render.js` cases): **Sand Slime** (desert surface, pre-HM + HM; drops SAND + GEL), **Giant Worm** (pre-HM underground pools; 5-segment `#c8b090` body via `initSegments` + `wyvernStep`), **Nymph** (HM deep underground; custom `nymphStep` — idles until the player is within 320px then flees with jumps; guaranteed Metal Detector drop), **Digger** (pre-HM + HM underground desert; 5-segment `#e0c878` body), **Crimslime** (HM Crimson surface + upper caves; drops CRIMTANE/ICHOR and splits into 2–3 **Slimelings** on death via a `killEntity` hook guarded by `e.splitDone`), **Slimeling** (small crimson slime, GEL drops), **Moth** (HM jungle surface at night; `flyStep`; 12% Butterfly Wings drop). Both worms initialize segments at both spawn sites (`initialSpawns`/`updateSpawning`, replaceAll) and use a new dedicated `drawWorm` renderer (the generic `drawWyvern` hardcodes colors, so per-species colors needed a dedicated path).
- **Metal Detector** (`I.METALDETECTOR`) — accessory guaranteed from Nymph. When equipped, `updateHud` appends "Nearest-Valuable Detected" to the biome line via new `equippedAccessory()`/`metalDetectorText()` helpers: a 42-tile-radius box scan over `world.get()` checks all ore tiles (Copper/Tin/Iron/Lead/Silver/Tungsten/Gold/Platinum/Demonite/Crimtane/Meteorite/Cobalt/Palladium/Mythril/Orichalcum/Adamantite/Titanium/Chlorophyte/Hellstone) plus Chest/Shadow Chest/Demon Altar/Plantera Bulb/Shadow Orb/Crimson Heart/Larva, reporting the nearest by squared distance or "Nothing Found" (`METAL_DETECTOR_NAMES` keyed by `T` id).
- **Butterfly Wings** (`I.BUTTERFLYWINGS`) — accessory `jumpMul:1.05, jumps:3, noFall:true` (the lightest wing tier, modeled on Mothron Wings), dropped 12% by Moths.
- **Smoke test: 60/60 checks PASS, zero console errors** (all 7 enum ids + 2 item ids/defs + 7 ENT_DEF entries, spawn + step + alive for each new enemy with segments verified for the worms, every new drop table resolves to defined items, Nymph guarantees the Metal Detector, Moth can drop Butterfly Wings, Crimslime split produces 2+ Slimelings, equipped-accessory/HUD/metal-detector text both with and without the accessory, updateHud + render, a deterministic world sweep across biomes/depths/day-night/pre+HM confirming Sand Slime/Giant Worm/Digger/Nymph/Moth are reachable in a corrupt world with no Crimslime leak, Crimslime reachable in a crimson world, all swept picks valid `ENT_DEF`, and 120 clean frames). Throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 60 - ambient enemies and critters (latest):**
- **Six hostile encounters completed** — Corrupt Slime, Spiked Jungle Slime, Spiked Ice Slime, and Umbrella Slime now join their biome/weather pools in both progression phases. Antlion Swarmers join Underground Desert pools, while seven-segment Dune Splicers are Hardmode-only there. Underground flying/worm types spawn around the player's depth rather than incorrectly at the surface.
- **Six passive critters completed** — Bunny, Bird, Squirrel, Frog, Goldfish, and Turtle use a separate 6-13 second daytime scheduler with a five-critter cap, so they neither consume the hostile cap nor accumulate indefinitely. Terrestrial critters require supported surface ground; rain-spawned Goldfish require a nearby two-tile-deep water habitat and swim without gravity while immersed.
- **Umbrella repaired** — the staged `I.UMBRELLA` now has a complete melee definition, restoring the `I`/`ITEMS` invariant. Selecting it clamps downward velocity and clears fall-distance accumulation. Umbrella Slimes retain their existing guaranteed acquisition route.
- **Rendering completed** — all twelve staged types have procedural render paths, including spiked slime crowns, Umbrella Slime canopy, Antlion wings, distinct small critter silhouettes, Goldfish, and per-species Dune Splicer segments.
- **Smoke test: 138 targeted checks PASS plus 120 clean generated-world frames** (661 base item ids/defs match before Army additions; all 12 definitions, pools, AI, drops, rendering, one-evil isolation, critter ground/water placement and cap, Umbrella behavior, and Dune segments verified). All JavaScript files pass `node --check`; no conflict markers or temporary test files remain.

**Tree collision parity repair (latest):**
- Ordinary forest and Jungle trees now generate as `T.TREETRUNK`, a mineable, Wood-dropping, non-solid tile. Leaves are also non-solid, matching Terraria traversal.
- `T.WOOD` remains solid for player construction, underground cabins, and Living Tree structures. Tree trunks retain the existing wood sprite, minimap color, mining particles, Wood drop, and Timber achievement path.
- New trunk generation starts above the surface instead of replacing the grass tile, preventing players from dropping one tile into the ground while passing through a tree.
- Saves now mark the new trunk format. Format-1 saves without the marker convert intact generated Wood columns and their old ground-level base while preserving chopped air and unrelated structural Wood.
- **Verification:** 10 generated-world collision/drop/structure checks, focused legacy migration coverage, 120 clean generated-world frames, save marker check, and full render pass. Every JavaScript file passes `node --check`.

**Terraria 1.4.4 parity Batch 61 - new enemies, items, and visual polish (latest):**
- **Three new enemies** (`const.js` `E` 295–297; `entities.js` `ENT_DEF` + AI + drops + `render.js` cases): **Spore Zombie** (jungle-surface Hardmode ranged walker; fires `SPIT`, drops `AMBER`), **Ice Elemental** (snow/ice Hardmode ranged walker; fires `FROSTBOLT`, drops ice-themed loot), and **Squid** (ocean/water enemy; swims and fires `SPIT`/`MAGICBOLT`). Both ranged walkers use the shared `rangedWalkerStep` (entities.js) with per-type cooldown/projectile/range, and fire whenever the target is within range regardless of ground contact.
- **Four new items** (`items.js` + `crafting.js`): **Flinx Staff** (`FLINXSTAFF`, summon staff spawning a flinx minion via `spawnMinion`), **Amber** (`AMBER` material, dropped by Antlions), **Amber Robe** (`AMBERROBE`, head armor), and **Amber Staff** (`AMBERSTAFF`, magic weapon cast through `Player.tryMagic` consuming mana). Recipes wire Amber Robe/Staff; Flinx Staff is a project substitute.
- **Visual polish** (`render.js`, `main.js`, `player.js`): screen-shake system (`game.shake`) on impacts/explosions, animated liquid surfaces (water/lava/honey shimmer), dawn/dusk twilight sky blending, star parallax/halo, walk-cycle bob, weapon swing trail, `hitEntity` hit-spark FX, and colored mine puffs via `spawnMinePuff(x,y,color)`; `tileColor` extended for the new content.
- **Smoke test: 101/101 checks PASS, zero console errors across 10+ consecutive headless runs** (S11 covers the 3 enemies spawning/stepping/firing, the flinx minion, Antlion→Amber drop, Amber Staff cast + mana drain, Amber Robe equip in the head slot, and I↔ITEMS consistency; S11b covers screen shake set/decay, hit-spark FX, colored mine puff, and twilight/night/liquid render paths). The test was initially flaky on the walker-firing assertion; root-caused to a **test artifact** (the pinned player sat at the world spawn, died to contact damage, `game.respawn()` → `clearSpawnHostiles()` removed the still-alive enemy from `game.entities`), not a game bug. Fixed by keeping the test player alive (invuln + full HP) and away from the spawn safe zone and asserting the fire via `attackCd` reset. Throwaway `test.html` deleted. All 14 JS files pass `node --check`.

**Terraria 1.4.4 parity Batch 62 - Expert/Master difficulty + world patch migration (latest):**
- **Difficulty modes** — `const.js` adds `DIFFICULTY` (`normal/expert/master` with `hp`, `dmg`, `coin`, `spawn`, `bag` multipliers) and a global `diffScale()` helper. `makeEntity` (`entities.js`) and `makeBoss` (`bosses.js`) scale enemy/boss `hp`/`dmg` by the difficulty; shared ranged helpers `rangedWalkerStep`/`ghostShooterStep` scale enemy projectile `dmg` too. Coin-type drops scale in `killEntity`, `killBoss`, and `game.openBossBag`. `updateSpawning` raises the active-enemy cap and shortens the spawn timer in harder modes.
- **Treasure Bags** — Expert/Master bosses no longer drop loot directly; `killBoss` (`bosses.js`) pushes an `I.BOSSBAG` pickup carrying `bagBoss` + `bagDrops`. `Inventory.add`/`addStack` (`items.js`) preserve the `bagBoss`/`bagDrops` extra fields through pickup→inventory. Opening a bag (left-click in the inventory grid → `game.openBossBag` in `main.js`) grants the (coin-scaled) loot. Normal mode keeps direct drops.
- **Menu + lifecycle** — `index.html` adds a Difficulty radio (mirrors the evil-biome selector). `buildGame(seed, evil, difficulty)` passes it to `World` and `game.difficulty`; `startNewGame` reads the radio; the choice is written to `saveSnapshot` (`difficulty`, `patch`) and restored by `applySaveData`. The HUD appends ` - <Mode> Mode` and the world-list menu shows the mode.
- **Minecraft-style migration** — `const.js` `PATCH = 1`. `migrateWorldPatch(data, loadedTiles, generatedTiles, width, game)` runs every upgrade step for `data.patch < PATCH`; the historical tree-trunk fix (`migrateLegacyTreeTrunks`) is now the `patch < 1` step, and a `while (patch < PATCH)` chain is the forward hook. Older saves (no `patch` field) are treated as patch 0 and migrate on load; future structural changes are added as new guarded steps. The `SAVE_FORMAT` hard gate is unchanged (serialization stays at 1). `bagBoss`/`bagDrops` survive save/load (inventory slots serialize as plain objects).
- **Smoke test: 16/16 checks PASS, zero console errors** (normal/enemy HP unscaled vs expert ×2 vs master ×3; boss HP ×2 in expert; expert boss drops a bag carrying boss id + loot; normal boss drops loot directly with no bag; `game.openBossBag` grants pickups and clears the stack; legacy tree-trunk migration runs for patch 0 and is skipped for patch 1; 120-frame crash-free step loop in both expert and master). Throwaway `test.html` deleted. All edited JS files (`const.js`, `items.js`, `entities.js`, `bosses.js`, `world.js`, `main.js`) pass `node --check`.

**Terraria 1.4.4 parity Batch 63 - difficulty fidelity:**
- **Reduced defense effectiveness** — `DIFFICULTY` now carries `defEff` (normal `1`, expert `0.5`, master `0.25`). `Player.damage` (`player.js:1190`) applies `def * 0.5 * defEff` instead of `def * 0.5`, so the player takes progressively more net damage in harder modes even with identical gear (the core 1.4.4 Expert/Master trait beyond enemy/boss stat scaling).
- **Increased knockback received** — `DIFFICULTY.kbMul` (normal `1`, expert `1.3`, master `1.6`) scales the knockback applied to the player in `Player.damage` (`this.vx += (kbx || 0) * kbMul`).
- **Death coin loss** — `DIFFICULTY.coinLoss` (expert `0.5`, master `0.75`, normal `0`) drains that fraction of `I.COIN` / `I.GOLD` / `I.PLATINUM` from the player's inventory on death via `Inventory.consume`, dropping the removed coins as pickups (`game.addPickup`); Normal keeps all coins. Implemented in `Player.die` (`player.js:1206`).
- **Boss Bag loot tuning** — Expert/Master boss bags (`killBoss` in `bosses.js:1604`) now also contain a guaranteed `I.GOLD` bonus (5–9) on top of the (coin-scaled) boss loot; coin counts still scale by `dm.coin` when the bag is opened via `game.openBossBag`.
- **Smoke test: 19/19 checks PASS** (difficulty table fields; normal/expert/master damage reduction is correct and monotonic; knockback ×1 / ×1.3 / ×1.6; master drops exactly 75% of each coin while pickups carry the rest; normal death keeps coins and drops nothing; expert boss drops a bag whose `bagDrops` includes bonus gold; normal boss drops loot directly with no bag). Only a benign `_guard/status` network-fetch notice appears; all JavaScript files pass `node --check`. Throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 64 - specialized combat behaviors (latest):**
- **Keybrand** — `I.KEYBRAND` now carries `nearbyBonus:0.15, nearbyMax:6, nearbyRadius:96`. `Player.tryMelee` (`player.js:495`) counts non-friendly enemies within `nearbyRadius` of the player and multiplies arc `mdmg` by `1 + nearbyBonus * min(count, nearbyMax)`, replicating the vanilla "hits harder surrounded by enemies" trait (verified 51 damage vs one enemy, 84 vs six, both >44 base).
- **KO Cannon** — `I.KOCANNON` is now `projectileOnly:true, meleeProj:P.KOBOMB, projBounces:3, explosive:60, projSpeed:9, projLife:3`. `Player.tryMelee` routes a non-arc `meleeProj` when present and forwards `explosive:def.explosive||0, gravity:def.projGravity||0` to the projectile, so the cannon fires a bouncing exploding orb instead of a sword swing.
- **The Horseman's Blade** — `I.THEHORSEMANSBLADE` gains `meleeProj:P.PUMPKINBLADE, projCount:2, projSpeed:7.5, projLife:2, projDamageMul:0.6, persistentProj:true`. `tryMelee` spawns two piercing spinning pumpkin blades (`persistent` → one hit per enemy via `hitEnemies`).
- **Influx Waver** — `I.INFLUXWAVER` gains `meleeProj:P.WAVERSWORD, projChance:0.34, projSpeed:10, projLife:1.6, projDamageMul:1`. `tryMelee` only emits the flying sword on a `Math.random() < projChance` roll (verified ≥1 hit in 80 swings).
- **Aqua Scepter** — `I.AQUASCEPTER` now uses `proj:P.WATERSTREAM, projPersistent:true, hitCooldown:0.25` (was a capped single projectile `P.SPIT`). `tryMagic` honors `projPersistent` so the stream pierces all enemies in its column.
- **Vilethorn** — `I.VILETHORN` now uses `proj:P.THORN, projPersistent:true, hitCooldown:0.2` (was `P.SPIT`), giving the vanilla piercing corrupt thorn instead of a stream of blobs.
- **Hellwing Bow** — `I.HELLWINGBOW` gains `batAmmo:true`. `Player.tryShoot` (`player.js:592`) converts the fired projectile to `P.BAT` with `homing:true` while still consuming the equipped `I.ARROW` (arrows still decrement).
- **Electrosphere Launcher** — `I.ELECTROSPHERELAUNCHER` gains `electro:true`. `tryShoot` adds `spawnSphere:!!def.electro` to the fired projectile; `finishProjectileEnemyHit` (`main.js:2172`) then spawns a `deployMode:'sphere'` `P.ELECTRO` electrosphere (`deployDamageMul:0.6, deployInterval:0.3, life:3`) on hit, mimicking the homing-sphere weapon.
- **Projectile visuals** — `render.js drawProjectile` (`render.js:4073`) adds `case` branches for `P.KOBOMB, P.PUMPKINBLADE, P.WAVERSWORD, P.WATERSTREAM, P.THORN, P.BAT, P.ELECTRO` (simple colored shapes); the `P` enum gained `KOBOMB:26, PUMPKINBLADE:27, WAVERSWORD:28, WATERSTREAM:29, THORN:30, BAT:31, ELECTRO:32` in `const.js`.
- **Smoke test: 16/16 checks PASS** (keybrand crowd scaling, KO bouncing explosive bomb, Horseman 2 piercing blades, Influx sword chance, Aqua piercing stream, Vilethorn piercing thorn, Hellwing bat conversion + arrow consumption, Electrosphere spawn-sphere on hit; all seven new `P` render paths present; only the benign `_guard/status` fetch notice appears; all edited JS files pass `node --check`). Throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 65 - vanilla enemy roster (latest):**
- **Eleven new classic enemies** (`const.js` `E` 298–308; `entities.js` `ENT_DEF` + AI + drops; `render.js` cases): **Skeleton** (forest-surface night walker, BONE/IRON drops), **Pinky** (rare pink slime, 3× GEL), **Mother Slime** (underground slime that splits into 2–3 **Blue Slimes** on death via a `killEntity` hook modeled on the Crimslime split), **Blue Slime** (small slime, 2× GEL), **Man Eater** (stationary jungle-ground vine; new `maneaterStep` fires a `P.STINGER` at the player within 360px on a 1.6s cooldown; VINE drop), **Medusa** (marble-cave flyer with snake-hair render; STONE/GOLD drops), **Undead Viking** (snow/undersnow walker; BONE/IRON drops), **Wall Warrior** (dungeon walker; BONE/IRON drops), **Spike Ball** (dungeon flyer; BONE drop), **Granite Elemental** (hardmode-only granite-cave flyer, diamond render; GRANITE drops), and **Basilisk** (desert sand-shark walker; SAND/GOLD drops). All reuse existing AI helpers (`zombieStep`/`slimeStep`/`flyStep`) except Man Eater's dedicated `maneaterStep`; all drops use already-defined materials (BONE/VINE/STONE/GRANITE/SAND/GOLD/IRON/GEL), so the Batch 57 acquisition audit stays clean with no new item defs.
- **Spawn-pool integration** (`main.js` `pickEnemy`) — forest surface/underground/deep pools gain Skeleton (night) / Pinky (rare) / Blue Slime / Mother Slime; jungle-underground branches gain Man Eater; marble branches gain Medusa; granite branches gain Granite Elemental (hardmode-only via `if (game.hardmode)`); snow + undersnow branches gain Undead Viking; dungeon pre/hardmode branches gain Wall Warrior + Spike Ball; desert/underdesert hardmode branches gain Basilisk. Pre-hardmode pools keep Granite Elemental out (hardmode-only, matching vanilla), while Medusa/Skeleton/etc. remain pre-hardmode-eligible for accuracy.
- **Smoke test: 34/34 checks PASS, zero console errors** (all 11 enum ids/defs, spawn+step+alive for each, flyers move, render for each with the mock canvas, Man Eater fires a `P.STINGER` enemy projectile, Mother Slime split produces Blue Slimes, `pickEnemy` returns valid ids across 60 randomized positions, and targeted reachability for Medusa-in-Marble, Granite-Elemental-in-Granite (hardmode), and Wall-Warrior-in-Dungeon). Only the benign `_guard/status` network-fetch notice appears; all four edited JS files pass the ES5 checker. Throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 66 - NPC services/quests (latest):**
- **Stylist hairstyling service** — the contextual Town panel for the Stylist (`E.STYLIST`) now shows a free **Change Hairstyle** service (`stylistHairHTML` / `stylistHair` in `main.js`) that cycles `player.hair` through nine styles; guarded by `townServiceNpcActive(E.STYLIST)` like the other NPC services. `Player.hair` defaults to 0 (constructor in `player.js`), round-trips through `saveSnapshot`/`applySaveData`, and renders as a colored hair tuft above the head in `drawPlayer` (`render.js`).
- **Angler Hotline Fishing Hook milestone** — `anglerQuestRewards(completion)` now grants a **Hotline Fishing Hook** (`I.HOTLINEFISHINGHOOK`, new accessory with `lavaFish:true`) when `completion === 25`. Equipping it lets `Player.tryFish` attach its bobber to lava (previously only water), and `rollFish` returns a crate from lava. Without the hook, lava casts still fail with "Cast into water!".
- **Reachability** — the Hook is granted only by the Angler quest reward path (scanned by the Batch 57 acquisition audit), so no new orphan item is introduced. No new recipes, drops, or chest loot were added.
- **Smoke test: 13/13 checks PASS, zero console errors** (new item def, hair default/cycle/wrap/save render, stylist proximity guard, Angler 25-vs-20 milestone reward, lava-fishing attach/yield with hook and refusal without hook). Only the benign `_guard/status` network-fetch notice appears; all five edited JS files pass the ES5 checker. Throwaway `test.html` deleted.

**Terraria 1.4.4 parity Batch 67 - Angler reward parity (latest):**
- **Classic milestone rewards** — added Fisherman's Pocket Guide (5 quests), Weather Radio (10), Sextant (15), Angler Earring (20), and Tackle Box (30) to the existing Hotline Fishing Hook milestone at 25. Rewards are deterministic, granted once at their exact lifetime-completion count, and pass through the Angler's existing atomic inventory-capacity simulation before the requested fish is consumed.
- **Functional fishing accessories** — `Inventory.accEffects()` now aggregates `fishingPower` and `baitSave`. Angler Earring adds 10 power to actual casts; Tackle Box gives a 5% chance not to consume bait, capped through the shared cast path. Ordinary casts without the accessories retain their prior power and consumption behavior.
- **Information accessories and Fish Finder** — Fisherman's Pocket Guide reports selected-rod/bait/buff/accessory fishing power, Weather Radio reports local conditions plus wind speed/direction, and Sextant reports an eight-phase moon cycle derived from `dayCount`. The craftable Fish Finder combines all three at an Anvil and exposes every readout through the existing biome HUD line.
- **Acquisition integrity** — all five component accessories are Angler-only rewards and Fish Finder has a single three-component recipe, so every new definition has a live acquisition path and the generic equipment Shimmer decraft map can reverse the combination without special cases.
- **Smoke test: 15/15 checks PASS** (six item definitions, exact Fish Finder recipe, every unique milestone plus non-milestone isolation, accessory aggregation, actual +10-power cast, deterministic bait save and ordinary-consumption regression, three information helpers, combined HUD, end-to-end Fish Finder craft, and an actual fifth quest turn-in). Only the benign `_guard/status` network-fetch notice appears; all JavaScript files pass `node --check` and `git diff --check` passes. Throwaway test files deleted.

**UI usability and terrain-generation repair (latest):**
- **Readable How To Play** — replaced two dense paragraphs with a six-section field guide covering controls, first-day goals, inventory/building, progression, travel, and events. The guide is an accessible dialog with its own sticky header, close button, and bounded scrolling; responsive checks keep it wholly inside both 1280×720 and 390×720 viewports.
- **Inventory drag-and-swap** — non-empty inventory cells are now draggable. Dropping onto another of the 50 slots calls the existing stack-safe `Inventory.swap`, preserving counts, reforges, Boss Bag metadata, and other per-stack fields. Source/target styling and a persistent inventory hint explain both dragging and equipment-slot clicks; hotbar rendering refreshes immediately after a move.
- **Spawn terrain invariant repaired** — the old flattening loop cleared from the desired surface upward, leaving the original Grass floating over an air cavity and recording an air tile as `surfaceY`. Fresh worlds now cut the old hill interval, fill valleys, normalize the new top to Grass, provide a twelve-tile solid foundation, clear generated trees above the actual local surface, and place both player and Guide correctly above solid ground.
- **Caves and chambers repaired** — random-walk caves no longer delete Water/Lava/Shimmer. Spider, Granite, Marble, and Aether pockets now create a material shell around a navigable air interior; the lower Aether basin fills with Shimmer while retaining open air above it.
- **Verification:** 13/13 real-page Playwright checks pass (desktop/mobile guide bounds and controls, real drag events and metadata preservation, and spawn/Guide/Aether/Ocean invariants across three full 1024×1000 generated worlds). All JavaScript files pass `node --check`; `git diff --check` passes; the only console notice is the expected blocked `file:///_guard/status` fetch. Throwaway harness deleted.

**Surface terrain and HUD hotfix (latest):**
- **Natural surface profile** — reduced the two surface-noise amplitudes and sine contribution so fresh worlds retain rolling terrain without the 20–30-tile cliffs visible around some spawn seeds. The 51-tile spawn clearing now blends into surrounding terrain across fifteen edge tiles instead of eight.
- **Continuous upper crust** — random-walk caves cannot carve within fourteen tiles of each column's recorded surface, while cave noise and deeper tunnels remain intact. The spawn clearing explicitly restores twelve solid tiles below its final top, eliminating the thin floating shelf exposed in the reported screenshot.
- **Correct layer HUD** — `depthString()` and `biomeName()` now measure from the player's feet. Normal ground level reports `Surface / Forest`; only positions more than 28 tiles above terrain report `Sky / Sky`, eliminating the contradictory ground-level `Forest (Sky)` readout.
- **Verification:** a twenty-seed full-world terrain audit reduced the tested spawn-area elevation range from as much as 27 tiles to at most 17, limited transitions to at most two tiles, and removed all tested spawn-foundation voids. A separate real-page Playwright suite passed 32/32 checks across four full 1600×640 worlds, including surface/slope/foundation, player/Guide support, HUD thresholds, and canvas rendering. All JavaScript files pass `node --check`; `git diff --check` passes; the throwaway harness was deleted.

**Terraria 1.4.4 parity Batch 68 - persistent Zoologist bestiary (latest):**
- **Persistent species records** — `game.bestiary` stores per-entry discovery and kill counts for a 250-entry catalog covering ordinary enemies, critters, event enemies, all implemented progression/event bosses, both Twins separately, each Lunar Pillar, and all three Old One's Army bosses. Town NPCs, duplicate technical boss entity IDs, and unreachable legacy Frost Zombie/Pirate Shark/Goblin Warlock definitions are excluded.
- **Real encounter lifecycle** — ordinary species unlock only when they enter the player's viewport rather than merely spawning elsewhere in the world. `game.onEnemyKilled` records normal kills, `killBoss` records boss kills (including first/second Twin deaths independently), repeated sightings do not change counts, and passive critters can be discovered without being treated as hostiles.
- **Save integrity** — bestiary records round-trip in `progress.bestiary`; restoration accepts only current catalog keys, clamps kill counts, preserves encountered boss stats, and treats a recorded kill as discovery. Older saves without the field load with an empty bestiary without a save-format change. Hosted join/reconnect snapshots inherit the same field through the existing `saveSnapshot()` transport.
- **Zoologist field guide** — the existing Town panel now includes discovered/total completion, total recorded kills, a progress bar, locked unknown entries, and unlocked name/type/HP/damage/defense/kill details. The responsive grid shares the panel's scrolling and remains within a 390×720 mobile viewport.
- **Verification:** 29/29 real-page Playwright checks pass (250 unique catalog entries, exclusions, nearby/offscreen/main-loop discovery, critters, ordinary kill idempotence, separate Twins + boss stats, save sanitization/round trip, old-save default, Zoologist summary/locked/full entry rendering, mobile width, and 120 clean gameplay frames plus render). All JavaScript files pass `node --check`; `git diff --check` passes; the throwaway harness was deleted.

**Player camera/render-state repair (latest):**
- **Root cause** — `drawPlayer()` saved and translated the shared canvas context for local body drawing but restored only its nested walk-bob state. Every frame therefore accumulated another player-position transform, leaving stale fragments from prior frames and presenting as duplicated players/suns, split terrain, and a drifting/broken camera.
- **Repair** — the outer player-local canvas state is restored before screen-space name, hook, fishing, kite, pet, light-pet, and weapon overlays. The camera center-coordinate contract and terrain data required no changes.
- **Verification:** 18/18 real-page Playwright checks pass at 1890x940, 1280x720, and 390x720, covering repeated camera movement, player centering, canvas resize, zero browser errors, and an identity canvas transform after every render. The reproduced 1890x940 scene now renders one continuous world with no stale duplicates. All 14 JavaScript files pass `node --check`; `git diff --check` passes; the throwaway probe and screenshot were deleted.

**Surface-profile camera/background repair (latest):**
- **Real-play diagnosis** — a full world created through the actual menu and traversed with real keyboard input showed that camera coordinates were synchronized, but `drawBackground()` still split the scene at one horizontal horizon taken from the center world column. On sloped terrain, lower neighboring surfaces therefore exposed large rectangular underground-colored bands behind above-ground valleys and trees.
- **Profile-aware layers** — surface rendering now paints the sky across the viewport, draws celestial elements behind the world, then masks the underground background from each visible column's own `surfaceY`. Valleys retain sky down to their real ground, hills hide the sky at their real crest, and cameras fully below the surface retain the dedicated underground/Underworld backgrounds.
- **Fresh production asset** — `index.html` versions the `render.js` request so browsers do not reuse the renderer cached before the camera repairs.
- **Verification:** the real menu/RAF gameplay probe created a world, used sustained right/left movement and jumps, captured four 1890x940 traversal states, resized live to 390x720, and passed 6/6 camera checks. The player stayed centered, every frame ended with an identity canvas transform, all 49 sampled valley pixels remained sky-colored, and no browser errors occurred. All 14 JavaScript files pass `node --check`; `git diff --check` passes; the throwaway probe was deleted.

**Terraria-style underground lighting (latest):**
- **Surface concealment** — the lighting mask follows each visible `surfaceY` column and becomes fully opaque one tile beneath the terrain top. Day/night and weather ambience still affect exposed surface air, but dirt, stone, caves, structures, enemies, and the player below ground are no longer visible through unlit darkness.
- **Real light sources** — the unconditional 110-pixel player aura was removed. Selecting a Torch or Glowstone now lights around the player; placed Torches/Glowstone, light pets, luminous projectiles, and Fallen Stars retain local illumination. Unlit underground cameras remain completely dark.
- **Emissive ores** — only Demonite, Crimtane, Meteorite, Hellstone, and Chlorophyte receive small partial light cuts and colored halos. Ordinary ores remain hidden until another light source reaches them.
- **Fresh production asset** — the `render.js` request version was advanced again so browsers do not reuse either pre-lighting renderer.
- **Verification:** 11/11 real-page checks pass for visible daytime sky, opaque surface subsurface, fully black unlit caves, absence of free player light, held Torch light, placed Torch light, the exact five-entry emissive-ore catalog, colored ore visibility with surrounding darkness, mobile cave darkness, identity canvas state, and zero browser errors. Final screenshots cover surface, dark cave, held/placed Torch, glowing ore, and 390x720 mobile states. All JavaScript files pass `node --check`; `git diff --check` passes; the throwaway probe was deleted.

**Missing / not done (future work):**
1. No automated test files retained — smoke testing used throwaway `test.html` (deleted); verify by running in a browser. Note: headless smoke test must drive the game via `buildGame(seed)` + `step(1/60)` since `requestAnimationFrame` doesn't fire under `--virtual-time-budget`. Also note: PowerShell 5.1 does NOT capture Chrome headless stdout via `& chrome ... 2>&1` or `> file` — use `Start-Process` with `-RedirectStandardOutput`, and the result div must exist **before** the test script runs (create it dynamically at the top of the script) or `getElementById('result')` returns null.
 2. Full Terraria 1.4.4 parity remains open-ended; see `PARITY.md`. Sixty-eight parity batches are complete, but comprehensive vanilla enemy, item, remaining NPC service/quest, specialized-behavior, and difficulty-mode audits remain.

---

## 7. Next steps (recommended order)

1. Open `index.html` in a browser and play a newly generated world: verify Torch-led cave exploration, smooth camera movement, and bestiary discovery across save/reload.
2. Continue Terraria 1.4.4 parity with Batch 69: audit Zoologist stock against bestiary-completion milestones and add the remaining functional bestiary-gated rewards that fit the current item catalog.
3. If anything throws, fix it (browser console is the guide). Quick re-verify path: use a throwaway real-page Playwright harness that calls `buildGame(seed)` directly; no test files are kept.
4. Update this file's Status/Next Steps when done.

---

## 8. Handoff instructions for the NEXT AI

If you are a fresh AI taking over:
1. Read `CONTEXT.md` (this file) completely.
2. Read the code files listed in [File Map](#file-map), especially `world.js`, `player.js`, `render.js`, `bosses.js`, `items.js`.
3. Check whether `js/main.js` exists. If not, build it (Section 4). If it exists, read it and continue from [Next Steps](#7-next-steps-recommended-order).
4. Never rewrite working subsystems; only extend. Preserve the ES5 style.
5. When your session ends, **update Status and Next Steps in this file** so the next session starts fresh from your last point.
